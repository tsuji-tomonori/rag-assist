# H-pre-gap

- 状態: done
- タスク種別: 調査
- branch: `codex/phase-h-pre-gap`
- worktree: `.worktrees/phase-h-pre-gap`
- base: `origin/main`

## 背景

Wave 4-pre の H-pre として、仕様 7/7A/7B/8 章と現行 `question-routes`、チャット回答不能/低評価、検索改善関連の実装・仕様 docs のギャップを調査する。

## 目的

後続 `H-support-search-improvement` が実装範囲を誤らないよう、現状で confirmed / partially covered / missing / divergent な項目、踏襲すべき既存挙動、scope / scope-out、認可境界と機微情報露出の注意点を docs に整理する。

## Scope

- 主な成果物:
  - `docs/spec/gap-phase-h.md`
  - `docs/spec/CHAPTER_TO_REQ_MAP.md`
  - 必要最小の `docs/spec-recovery/` 関連 docs
  - `reports/working/20260514-1932-h-pre-gap.md`
- 調査対象:
  - 仕様 7/7A/7B/8 章
  - `apps/api/src/routes/question-routes.ts`
  - `apps/api/src/routes/chat-routes.ts`
  - `apps/api/src/chat-orchestration/`
  - `apps/api/src/rag/`
  - `apps/api/src/security/access-control-policy.test.ts`
- 原則としてコード変更は行わない。

## Documentation Maintenance Plan

- 既存 docs の章 ID / 要件 ID を維持し、必要最小の追記で更新する。
- 確定事実と推定・未確定点を `confirmed`、`partially covered`、`missing`、`divergent`、`open_question` として区別する。
- SupportTicket、sanitize 済み trace、検索改善 AI suggest + human review、UI 上 alias 非露出方針、認可境界を明示的に含める。

## 受け入れ条件

- [x] `docs/spec/gap-phase-h.md` に、仕様 7/7A/7B/8 章 vs 現行実装の confirmed / partially covered / missing / divergent が記録されている。
- [x] `docs/spec/gap-phase-h.md` に、踏襲すべき既存挙動と後続 `H-support-search-improvement` の scope / scope-out が記録されている。
- [x] `docs/spec/CHAPTER_TO_REQ_MAP.md` と必要最小の `docs/spec-recovery/` 関連 docs が 7/7A/7B/8・検索改善・問い合わせ対応の観点で更新されている。
- [x] SupportTicket、sanitize 済み trace、検索改善 AI suggest + human review、UI 上 alias 非露出方針、認可境界が調査観点に含まれている。
- [x] `reports/working/20260514-1932-h-pre-gap.md` が作成されている。
- [x] `python3 scripts/validate_spec_recovery.py docs/spec-recovery` が pass している。
- [x] `git diff --check` が pass している。
- [x] PR が作成され、受け入れ条件コメントとセルフレビューコメントが投稿されている。
- [x] PR コメント後に task md が `tasks/done/` へ移動され、状態が `done` になり、同じ branch に commit / push されている。

## 検証計画

- `python3 scripts/validate_spec_recovery.py docs/spec-recovery`
- `git diff --check`

## PR Review Points

- H-pre が実装を変更せず、後続タスクの調査 docs に範囲を閉じていること。
- 認可境界、機微情報、alias 非露出、debug/trace sanitize の注意点が漏れていないこと。
- 未実装・未検証事項を実装済みとして扱っていないこと。

## リスク

- 仕様章の一部が既存 docs とコードに分散しており、確定できない項目は open question として残す必要がある。
- 並行 worker の変更と重なる可能性があるため、専用 worktree の範囲外は変更しない。
