# G-pre gap 調査

- 状態: done
- タスク種別: 調査
- 作業ブランチ: `codex/phase-g-pre-gap`
- 対象 PR: https://github.com/tsuji-tomonori/rag-assist/pull/311

## 背景

plan の Wave7-pre として、Phase G / 仕様 4C「非同期エージェント実行」の現仕様と現実装の差分を整理する。後続の `G1-async-agent-foundation`、`G2-async-agent-claude-code`、`G3-async-agent-codex`、`G4-async-agent-opencode` を小さく分割して進められるよう、流用できる基盤、未実装要素、弱めてはいけない境界を明確化する。

## 目的

- 仕様 4C と現実装の confirmed / partially_covered / missing / divergent を整理する。
- Phase B/C/F/I/J2 の成果物との依存関係と保持すべき境界を明文化する。
- 後続 Phase G 実装 PR の分割前提、scope、scope-out、open questions、targeted validation を記録する。

## Scope

- `docs/spec/2026-chapter-spec.md` の 4C 章。
- API 型/schema/RAG service/chat worker/routes/security policy。
- Web features、contract package、infra CDK、benchmark CodeBuild suite、既存 gap docs。
- 成果物は docs/task/report のみ。ランタイム実装は行わない。

## Plan

1. 仕様 4C と関連 gap docs を読む。
2. API/Web/infra/benchmark/contract の関連実装を確認する。
3. `docs/spec/gap-phase-g.md` を作成する。
4. `docs/spec/CHAPTER_TO_REQ_MAP.md` と `docs/spec-recovery/08_traceability_matrix.md` に必要最小限の trace を追記する。
5. 指定検証を実行し、作業レポートを作成する。
6. PR 作成後に受け入れ条件確認コメントとセルフレビューコメントを投稿し、task を done へ移動する。

## Documentation Maintenance Plan

- 新規 gap doc と既存 trace index を更新する。
- README / API examples / operations docs は、docs-only の調査成果でランタイム・運用手順を変えないため更新不要と判断する。

## 受け入れ条件

- [x] `docs/spec/gap-phase-g.md` に 4C / Phase G の confirmed / partially_covered / missing / divergent / preserve existing behavior / implementation scope / scope-out / open questions / targeted validation が整理されている。
- [x] `async_agent_run` trace target / worker target 型、benchmark `async_agent_task` artifact contract、CodeBuild runner / Secrets / IAM 基盤の流用可否が整理されている。
- [x] 未実装の `AsyncAgentRun`、provider adapter、workspace mount、writeback approval、SkillDefinition、AgentProfileDefinition、preset、provider credentials/Secrets、budget/timeout/tool-call enforcement、Web UI が整理されている。
- [x] B/C/F/I/J2 の依存と、folder permission、quality-approved evidence、tool approval/audit、debug tier、benchmark corpus isolation、No Mock Product UI、Secrets 管理の保持方針が明記されている。
- [x] 後続 `G1-async-agent-foundation`、`G2-async-agent-claude-code`、`G3-async-agent-codex`、`G4-async-agent-opencode` の sub-PR 分割前提が明記されている。
- [x] `docs/spec/CHAPTER_TO_REQ_MAP.md` に 4C / Phase G の trace が必要最小限追記されている。
- [x] `docs/spec-recovery/08_traceability_matrix.md` に Phase G-pre の trace が必要最小限追記されている。
- [x] 作業レポート `reports/working/20260514-2313-g-pre-gap.md` が作成されている。

## 完了時検証

- `python3 scripts/validate_spec_recovery.py docs/spec-recovery`: pass（通常メッセージとして warning review の案内あり）
- `git diff --check`: pass
- API/Web unit/typecheck は docs/task/report のみの変更で runtime API / Web / infra / contract code を変更していないため未実施。

## Validation Plan

- `python3 scripts/validate_spec_recovery.py docs/spec-recovery`
- `git diff --check`
- API/Web tests は docs-only のため原則未実施とし、理由を report / PR に記録する。

## PR Review Points

- 後続実装で security / RAG quality / benchmark isolation 境界を弱める scope になっていないか。
- confirmed と inferred / missing / open_question が混ざっていないか。
- 後続 sub-PR の分割が実装可能な粒度か。

## Risks

- 4C の provider ごとの実装詳細は未実装のため、provider adapter の保存 schema や Secrets 名は後続設計で確定が必要。
- Web UI と API の最小契約を同時に決める必要があり、G1 の設計次第で G2-G4 の順序が変わる可能性がある。
