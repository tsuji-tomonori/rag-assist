# B-pre 認可ギャップ調査
状態: doing
タスク種別: 調査
発注元 wave: Wave 2-pre
依存タスク: `tasks/done/20260514-1455-a3-cleanup-stale-mvp-dir.md`
仕様参照:
  - `docs/spec/2026-chapter-spec.md` の章 16 / 17 / 18 / 19 / 20 / 21 / 21A
  - `docs/spec/CHAPTER_TO_REQ_MAP.md` の 16 / 17 / 18 / 19 / 20 / 21 / 21A

## 背景

Wave 2 の `B-authorization-3layer` は、認可基盤を後続全 wave の前提として再構成する単独ブロッカーである。

実装前に、章 16-21A の仕様と現行 `apps/api/src/authorization.ts`、route-level policy、`access-control-policy.test.ts` の差分を確定し、踏襲すべき既存挙動を明文化する必要がある。

## スコープ

- 含む:
  - 仕様章 16/17/18/19/20/21/21A の要求・用語・認可表の棚卸し。
  - 現行 roles / permissions / route authorization modes / ownership checks / error message の棚卸し。
  - `docs/spec/gap-phase-b.md` の作成。
  - 後続 `B-authorization-3layer` に渡す implementation input と validation plan の整理。
- 含まない:
  - `apps/api/src/authorization.ts` の実装変更。
  - route handler の認可ロジック変更。
  - OpenAPI schema / contract の変更。

## 実装計画

1. 仕様章 16/17/18/19/20/21/21A と `CHAPTER_TO_REQ_MAP.md` の該当行を確認する。
2. `authorization.ts`、`access-control-policy.test.ts`、route files の現行認可実装を確認する。
3. 差分を `confirmed` / `inferred` / `open_question` に分けて整理する。
4. `docs/spec/gap-phase-b.md` を作成し、踏襲すべき既存挙動と後続実装 task の受け入れ条件候補を残す。
5. `scripts/validate_spec_recovery.py` の適用可否を確認し、必要な検証を実行する。
6. 作業完了レポートを `reports/working/` に作成する。
7. PR 作成後、受け入れ条件確認コメントとセルフレビューコメントを入れる。
8. task md を `tasks/done/` へ移動し、同じ PR branch に commit / push する。

## ドキュメント更新計画

- `docs/spec/gap-phase-b.md`: Phase B の仕様差分、踏襲挙動、後続実装入力を追加。
- `reports/working/20260514-1506-b-pre-gap.md`: 作業完了レポートを追加。

## 受け入れ条件 (acceptance criteria)

- [x] 章 16/17/18/19/20/21/21A の主要要求が `docs/spec/gap-phase-b.md` に整理されている。
  - 根拠: `docs/spec/gap-phase-b.md` の `Spec Requirements Summary`。
- [x] 現行 roles / permissions / route authorization modes / ownership checks / 403 message の踏襲事項が記録されている。
  - 根拠: `Current Implementation Summary` と `Preserve Existing Behavior`。
- [x] `B-authorization-3layer` の implementation input と targeted validation が記録されている。
  - 根拠: `B-authorization-3layer Implementation Input` と `Targeted Validation For B`。
- [x] `python3 scripts/validate_spec_recovery.py docs/spec-recovery` の実施または未実施理由が記録されている。
  - 根拠: pass。出力は `Validation completed. Review warnings before treating the spec recovery as complete.`。
- [x] `git diff --check` が pass している。
  - 根拠: `git diff --check` pass。

## 検証計画

- `python3 scripts/validate_spec_recovery.py docs/spec-recovery`
- `git diff --check`
- 必要に応じて `npm exec -w @memorag-mvp/api -- tsx --test src/security/access-control-policy.test.ts`

## PR レビュー観点

- 章 20 の操作別最終認可表を、後続実装 task が実装可能な粒度で参照できるか。
- 現行の owner / requester / benchmark seed 例外を落としていないか。
- エラー文言の存在示唆除去を、未実装のまま完了扱いしていないか。
- 章 21 の RAG 認可不変条件と route-level 認可の境界を混同していないか。

## リスク・open questions

- 章 20 の操作表が現行 route 数より広いため、B 実装で一度に API surface を増やすのではなく、既存 route の policy 強化と未実装操作の planning 化に分ける必要がある可能性が高い。
- 章 21A は J1/J2 とも依存するため、B-pre では API lifecycle 全体の実装は扱わず、認可 metadata / error 互換性の観点に限定する。
