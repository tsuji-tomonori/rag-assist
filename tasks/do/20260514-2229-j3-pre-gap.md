# J3-pre-gap 調査

- 状態: do
- タスク種別: 調査
- branch: `codex/phase-j3-pre-gap`
- worktree: `.worktrees/phase-j3-pre-gap`
- base: `origin/main`

## 背景

Wave 6-pre `J3-pre-gap` worker として、後続 `J3-admin-dashboard-unified` の前提になる仕様 10/11/12/13/14 章と admin 実装/API のギャップ・影響範囲を整理する。

## 目的

`docs/spec/2026-chapter-spec.md` の 10/11/12/13/14 章に対し、`apps/web/src/features/admin/` と admin API routes の現状を確認し、後続実装の scope/scope-out と維持すべき認可・UI 制約を docs に残す。

## Scope

- 仕様章: `docs/spec/2026-chapter-spec.md` 10/11/12/13/14
- Web: `apps/web/src/features/admin/`
- API/Auth: `apps/api/src/routes/admin-routes.ts`, `apps/api/src/routes/system-routes.ts`, `apps/api/src/authorization.ts`, `apps/api/src/security/access-control-policy.test.ts`
- Admin usage/cost/audit/role/user API と generated web inventory
- Docs: `docs/spec/gap-phase-j3.md`, `docs/spec/CHAPTER_TO_REQ_MAP.md`, 必要に応じて `docs/spec-recovery/08_traceability_matrix.md`

## 実施計画

1. 仕様 10/11/12/13/14 章と admin 関連実装/API の現状を読む。
2. confirmed / partially_covered / missing / divergent でギャップを分類する。
3. No Mock Product UI、既存 permission 境界、debug/public/auth middleware 境界、benchmark artifact contract の影響を確認する。
4. `docs/spec/gap-phase-j3.md` と `docs/spec/CHAPTER_TO_REQ_MAP.md` を必要最小限更新する。
5. 適用可能なら `scripts/validate_spec_recovery.py docs/spec-recovery` と `git diff --check` を実行する。
6. 作業レポート、commit/push、PR 作成、受け入れ条件確認コメント、セルフレビューコメント、task done 移動まで行う。

## ドキュメントメンテ計画

- 実装変更ではなく調査/docs 更新として扱い、README/API 実装 docs への変更は必要性を確認して不要なら作業レポートと PR 本文に理由を残す。
- `docs/spec/gap-phase-j3.md` には後続 `J3-admin-dashboard-unified` の最小 slice と scope-out を明記する。
- `docs/spec/CHAPTER_TO_REQ_MAP.md` は 10/11/12/13/14 の trace 追記に限定し、既存 ID を不必要に改番しない。

## 受け入れ条件

- [ ] `docs/spec/gap-phase-j3.md` に仕様 10/11/12/13/14 と admin 実装/API の confirmed / partially_covered / missing / divergent が記録されている。
- [ ] 踏襲すべき既存挙動として、admin permission 境界、debug 4 tier/public/auth middleware 境界、No Mock Product UI、benchmark artifact read-only 方針が記録されている。
- [ ] 後続 `J3-admin-dashboard-unified` の admin workspace 統合表示 scope 候補、最小 slice、scope-out が整理されている。
- [ ] `docs/spec/CHAPTER_TO_REQ_MAP.md` に 10/11/12/13/14 の trace が必要最小限追記されている。
- [ ] 適用可能なら `scripts/validate_spec_recovery.py docs/spec-recovery` と `git diff --check` が実行され、結果または未実施理由が記録されている。
- [ ] 作業レポート、commit/push、PR 作成、日本語の受け入れ条件確認コメント、セルフレビューコメント、task done 移動と追加 commit/push が完了している。

## 検証計画

- `scripts/validate_spec_recovery.py docs/spec-recovery`
- `git diff --check`
- docs-only 変更のため、API/Web の unit/typecheck は差分に応じて不要判断を明記する。

## PR レビュー観点

- 仕様章から admin 実装/API への trace が根拠付きで読めること。
- 既存 route permission や public/debug/auth 境界を弱める提案になっていないこと。
- 本番 UI/API に固定ユーザー、固定グループ、固定容量、demo fallback を入れるような scope が含まれていないこと。
- benchmark artifact contract を admin scope に混ぜる場合でも read-only 表示・artifact 参照に限定していること。

## リスク

- 仕様章の粒度と現実装の粒度が一致しない場合、`inferred` または `open_question` として扱い、後続実装で過剰 scope にならないよう scope-out を明記する。
- docs-only 調査のため、実装挙動の runtime 検証は行わない。必要な route permission は静的ファイル確認で根拠を残す。
