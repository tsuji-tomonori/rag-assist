<!-- This file is generated by npm run docs:api-code. Do not edit manually. -->

# POST /documents/{documentId}/source-governance/approve メッセージ仕様

## 生成根拠

- OpenAPI response description
- handler と到達関数の `c.json({ error/message })`
- `HTTPException` / `Error` の literal message
- logger call と SSE event の literal message

## メッセージ一覧

| ID | 種別 | Status/Event | メッセージ | 発生条件 | 実装根拠 |
| --- | --- | --- | --- | --- | --- |
| M001 | OpenAPI contract | `200` | リクエストは成功し、レスポンス body に結果を返します。 | OpenAPI で宣言された HTTP 200 response | runtime OpenAPI |
| M002 | OpenAPI contract | `400` | リクエスト形式または入力値が不正です。 | OpenAPI で宣言された HTTP 400 response | runtime OpenAPI |
| M003 | OpenAPI contract | `401` | 認証が必要です。 | OpenAPI で宣言された HTTP 401 response | runtime OpenAPI |
| M004 | OpenAPI contract | `403` | 対象操作を実行する権限がありません。 | OpenAPI で宣言された HTTP 403 response | runtime OpenAPI |
| M005 | OpenAPI contract | `404` | 指定したリソースが見つかりません。 | OpenAPI で宣言された HTTP 404 response | runtime OpenAPI |
| M006 | OpenAPI contract | `409` | 現在のリソース状態と要求された操作が競合しています。 | OpenAPI で宣言された HTTP 409 response | runtime OpenAPI |
| M007 | OpenAPI contract | `503` | 監査、identity、staging、または公開処理を利用できない | OpenAPI で宣言された HTTP 503 response | runtime OpenAPI |
| M008 | 例外 | `403` | Forbidden | 利用者が 指定された permission を持たない | `apps/api/src/authorization.ts:184 (requirePermission)` |
| M009 | 例外 | `-` | Document identifier is required | `normalized` が存在しない、または偽である | `apps/api/src/rag/_shared/storage/tenant-artifacts.ts:133 (encodeIdentifier)` |
| M010 | 例外 | `-` | Artifact key is invalid | `normalized` が存在しない、または偽である、または `normalized` が ".." を含む | `apps/api/src/rag/_shared/storage/tenant-artifacts.ts:127 (normalizeRelativeKey)` |
| M011 | 例外 | `-` | Authoritative tenant is required | `normalized` が存在しない、または偽である | `apps/api/src/rag/_shared/storage/tenant-artifacts.ts:121 (requiredTenantId)` |
| M012 | 例外 | `-` | Authoritative tenant is required | `normalized` が存在しない、または偽である | `apps/api/src/security/tenant-partition.ts:5 (tenantPartitionId)` |
| M013 | 例外 | `-` | `Document manifest tenant mismatch${key ? `: ${key}` : ""}` | `manifestTenantId` が存在しない、または偽である、かつ `options.allowMissingTenant` が存在しない、または偽である、または `manifestTenantId` が存在し、真である、かつ `manifestTenantId` が `normalizedTenantId` と異なる | `apps/api/src/rag/_shared/storage/tenant-artifacts.ts:111 (assertManifestTenant)` |
| M014 | 例外 | `-` | worker_tenant_missing | `tenantId` が存在しない、または偽である | `apps/api/src/rag/memorag-service.ts:4797 (MemoRagService.assertCurrentWorkerAuthorization)` |
| M015 | 例外 | `-` | worker_identity_not_canonical | canonical の判定結果が真ではない、または canonical の判定結果が真ではない、または canonical の判定結果が真ではない | `apps/api/src/security/current-worker-authorization.ts:46 (CurrentWorkerAuthorization.assertAuthorized)` |
| M016 | 例外 | `-` | authoritative_identity_unavailable | 例外を捕捉した場合 | `apps/api/src/security/current-worker-authorization.ts:53 (CurrentWorkerAuthorization.assertAuthorized)` |
| M017 | 例外 | `-` | account_deleted | `identity` が存在しない、または偽である | `apps/api/src/security/current-worker-authorization.ts:55 (CurrentWorkerAuthorization.assertAuthorized)` |
| M018 | 例外 | `-` | subject_mismatch | `identity.userId` が `request.subject` と異なる | `apps/api/src/security/current-worker-authorization.ts:56 (CurrentWorkerAuthorization.assertAuthorized)` |
| M019 | 例外 | `-` | account_inactive | `identity.accountStatus` が `"active"` と異なる | `apps/api/src/security/current-worker-authorization.ts:57 (CurrentWorkerAuthorization.assertAuthorized)` |
| M020 | 例外 | `-` | tenant_membership_revoked | `identity.tenantId` が `request.tenantId` と異なる | `apps/api/src/security/current-worker-authorization.ts:58 (CurrentWorkerAuthorization.assertAuthorized)` |
| M021 | 例外 | `-` | role_permission_revoked | every の判定結果が真ではない | `apps/api/src/security/current-worker-authorization.ts:69 (CurrentWorkerAuthorization.assertAuthorized)` |
| M022 | 例外 | `-` | resource_policy_revoked | `resourceAllowed` が存在しない、または偽である | `apps/api/src/security/current-worker-authorization.ts:78 (CurrentWorkerAuthorization.assertAuthorized)` |
| M023 | 例外 | `-` | authoritative_identity_provider_missing | `config.authEnabled` が存在し、真である | `apps/api/src/rag/memorag-service.ts:4806 (MemoRagService.assertCurrentWorkerAuthorization)` |
| M024 | 例外 | `-` | local_fixture_permission_missing | `config.nodeEnv` が `"test"` と異なる、かつ every の判定結果が真ではない | `apps/api/src/rag/memorag-service.ts:4816 (MemoRagService.assertCurrentWorkerAuthorization)` |
| M025 | 例外 | `-` | local_fixture_resource_policy_denied | `resourceAllowed` が存在しない、または偽である | `apps/api/src/rag/memorag-service.ts:4824 (MemoRagService.assertCurrentWorkerAuthorization)` |
| M026 | 例外 | `-` | Publication scope is incomplete | `normalized.tenantId` が存在しない、または偽である、または `normalized.actorId` が存在しない、または偽である、または `normalized.sourceId` が存在しない、または偽である、または `normalized.sourceVersion` が存在しない、または偽である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:1786 (normalizeScope)` |
| M027 | 例外 | `-` | Active publication pointer scope mismatch | `existing.value.scopeHash` が `scopeHash` と異なる、または `existing.value.sourceId` が `scope.sourceId` と異なる | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:912 (StagedPublicationCoordinator.ensureBootstrapPointer)` |
| M028 | 例外 | `-` | Active publication pointer not found | `pointer` が存在しない、または偽である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:1495 (StagedPublicationCoordinator.getActivePointer)` |
| M029 | 例外 | `-` | Source manifest is missing | `stored` が存在しない、または偽である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:956 (StagedPublicationCoordinator.annotateManifestWithPublicationControl)` |
| M030 | 例外 | `-` | Could not annotate source manifest with publication control | 当該処理へ到達した場合 | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:970 (StagedPublicationCoordinator.annotateManifestWithPublicationControl)` |
| M031 | 例外 | `-` | Publication idempotency state is inconsistent | `stored` が存在しない、または偽である、または `stored.value.scopeHash` が `identity.scopeHash` と異なる、または `stored.value.idempotencyKey` が `identity.idempotencyKey` と異なる | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:229 (StagedPublicationCoordinator.begin)` |
| M032 | 例外 | `-` | Publication run not found | `stored` が存在しない、または偽である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:591 (StagedPublicationCoordinator.acquireLease)` |
| M033 | 例外 | `-` | `Publication run is ${stored.value.status}` | `forCommit` が存在し、真である、かつ `stored.value.status` が `"validated"` と異なる、かつ `stored.value.status` が `"committing"` と異なる | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:602 (StagedPublicationCoordinator.acquireLease)` |
| M034 | 例外 | `-` | `Publication run is ${stored.value.status}` | `forCommit` が存在しない、または偽である、かつ `stored.value.status` が `"initialized"` と異なる、かつ `stored.value.status` が `"staging"` と異なる | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:603 (StagedPublicationCoordinator.acquireLease)` |
| M035 | 例外 | `-` | Publication run disappeared after lease acquisition | `nextStored` が存在しない、または偽である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:619 (StagedPublicationCoordinator.acquireLease)` |
| M036 | 例外 | `-` | `Source governance publication run is ${begun.run.status}` | `begun.run.stagedArtifact` が存在しない、または偽である | `apps/api/src/rag/memorag-service.ts:3848 (MemoRagService.stageApprovedSourceGovernancePublication)` |
| M037 | 例外 | `-` | Document manifest escaped its authoritative tenant partition | uses legacy global document artifacts の判定結果が真ではない、かつ starts with の判定結果が真ではない | `apps/api/src/rag/_shared/storage/tenant-artifacts.ts:96 (readTenantManifestByKey)` |
| M038 | 例外 | `-` | Source governance publication lease was not acquired | `begun.lease` が存在しない、または偽である | `apps/api/src/rag/memorag-service.ts:3855 (MemoRagService.stageApprovedSourceGovernancePublication)` |
| M039 | 例外 | `-` | Source identity, tenant, owner, or version is missing | `sourceId` が存在しない、または偽である、または `sourceVersion` が存在しない、または偽である、または `tenantId` が存在しない、または偽である、または `ownerUserId` が存在しない、または偽である | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:872 (sourceIdentity)` |
| M040 | 例外 | `-` | Source authorization or provenance authority is missing | `admission?.authorizationRef` が存在しない、または偽である、または `admission.provenanceRef` が存在しない、または偽である | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:685 (createApprovedSourceAdmissionContext)` |
| M041 | 例外 | `-` | Invalid staged publication fence | `fence.runId` が存在しない、または偽である、または `fence.artifactId` が存在しない、または偽である、または `fence.idempotencyKey` が存在しない、または偽である、または `fence.sourceId` が存在しない、または偽である、または `fence.fencingToken` が存在しない、または偽である、または is integer の判定結果が真ではない、または `fence.generation` が `0` 以下である、または starts with の判定結果が真ではない、または `fence.stageNamespace` が ".." を含む | `apps/api/src/rag/offline/pre-retrieval/ingestion/ingest-run.service.ts:669 (assertPublicationFence)` |
| M042 | 例外 | `-` | RAG safety state is unavailable. | `required` が存在し、真である | `apps/api/src/rag/quality-control/production-rag-monitor.ts:313 (assertRagSafetyInterlock)` |
| M043 | 例外 | `-` | RAG safety state is invalid or unsupported. | `state.schemaVersion` が `1` と異なる、または is integer の判定結果が真ではない、または `state.stateVersion` が `1` より小さい、または trim の判定結果が真ではない、または trim の判定結果が真ではない、または trim の判定結果が真ではない、または is array の判定結果が真ではない、または some の判定結果が真である、または `typeof state.promotionFrozen` が `"boolean"` と異なる、または `typeof state.documentQuarantineRequired` が `"boolean"` と異なる、または `["normal", "limited", "refuse"]` が state.responseMode を含まない、または is finite の判定結果が真ではない、または is finite の判定結果が真ではない、または `Date.parse(state.validUntil)` が `Date.parse(state.updatedAt)` 以下である | `apps/api/src/rag/quality-control/production-rag-monitor.ts:362 (assertRagSafetyState)` |
| M044 | 例外 | `-` | RAG safety state is expired. | is finite の判定結果が真ではない、または `Date.parse(state.validUntil)` が `Date.parse(now)` 以下である | `apps/api/src/rag/quality-control/production-rag-monitor.ts:319 (assertRagSafetyInterlock)` |
| M045 | 例外 | `-` | The requested RAG runtime is not the active monitored runtime. | `state.activeRuntimeProfileVersion` が `input.runtimeProfileVersion` と異なる | `apps/api/src/rag/quality-control/production-rag-monitor.ts:322 (assertRagSafetyInterlock)` |
| M046 | 例外 | `-` | `RAG ${state.responseMode} response mode is active.` | `operation` が `"chat"` と等しい、または `operation` が `"search"` と等しい、かつ `state.responseMode` が `"normal"` と異なる | `apps/api/src/rag/quality-control/production-rag-monitor.ts:329 (assertRagSafetyInterlock)` |
| M047 | 例外 | `-` | RAG promotion is frozen by the monitoring control loop. | `operation` が `"publication"` と等しい、または `operation` が `"promotion"` と等しい、かつ `state.promotionFrozen` が存在し、真である | `apps/api/src/rag/quality-control/production-rag-monitor.ts:332 (assertRagSafetyInterlock)` |
| M048 | 例外 | `-` | RAG document publication is blocked while quarantine is required. | `operation` が `"publication"` と等しい、かつ `state.documentQuarantineRequired` が存在し、真である | `apps/api/src/rag/quality-control/production-rag-monitor.ts:335 (assertRagSafetyInterlock)` |
| M049 | 例外 | `-` | Current ingest authorization callbacks are required | `config.authEnabled` が存在し、真である、または `config.nodeEnv` が `"production"` と等しい、かつ `input.currentAuthorization` が存在しない、または偽である | `apps/api/src/rag/offline/pre-retrieval/ingestion/ingest-run.service.ts:74 (runIngestPipeline)` |
| M050 | 例外 | `-` | Internal artifact identity is invalid | `normalized` が `value` と異なる、または test の判定結果が真ではない | `apps/api/src/rag/offline/pre-retrieval/ingestion/ingest-run.service.ts:677 (canonicalArtifactId)` |
| M051 | 例外 | `-` | Authoritative tenant is required before document artifact persistence | `tenantId` が存在しない、または偽である | `apps/api/src/rag/offline/pre-retrieval/ingestion/ingest-run.service.ts:90 (runIngestPipeline)` |
| M052 | 例外 | `-` | Either text or contentBase64, or contentBytes is required | `input.contentBase64` が存在しない、または偽である、かつ `input.contentBytes` が存在しない、または偽である | `apps/api/src/rag/offline/pre-retrieval/extraction/text-extractor.ts:114 (extractDocumentFromUpload)` |
| M053 | 例外 | `-` | OCR fallback blocked because mandatory guard outcomes were not observed | `input.enforceObservedFallbackGuards` が存在し、真である、かつ `ocrFallbackWarning.degradationDecision?.action` が `"fail"` と等しい | `apps/api/src/rag/offline/pre-retrieval/extraction/text-extractor.ts:341 (extractPdfDocument)` |
| M054 | 例外 | `-` | Uploaded document did not contain extractable text | `text` が存在しない、または偽である | `apps/api/src/rag/offline/pre-retrieval/ingestion/ingest-run.service.ts:119 (runIngestPipeline)` |
| M055 | 例外 | `-` | No chunks were produced from the uploaded document | `chunks.length` が `0` と等しい | `apps/api/src/rag/offline/pre-retrieval/ingestion/ingest-run.service.ts:135 (runIngestPipeline)` |
| M056 | 例外 | `-` | Embedding cache partition is required | trim の判定結果が真ではない | `apps/api/src/rag/offline/pre-retrieval/embedding/embedding-cache.ts:17 (embedWithCache)` |
| M057 | 例外 | `-` | Vector key is required | `normalizedKey` が存在しない、または偽である | `apps/api/src/rag/_shared/storage/tenant-artifacts.ts:71 (tenantVectorKey)` |
| M058 | 例外 | `-` | Failed ingest cleanup has no authoritative tenant for reconciliation | `tenantId` が存在しない、または偽である | `apps/api/src/rag/offline/pre-retrieval/ingestion/ingest-run.service.ts:537 (registerUncommittedIngestCleanupReconciliation)` |
| M059 | 例外 | `-` | `${field} is invalid` | `normalized` が存在しない、または偽である、または `normalized.length` が `512` より大きい、または `hasControlCharacter` が存在し、真である | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:651 (canonicalIdentifier)` |
| M060 | 例外 | `-` | `${field} is invalid` | is finite の判定結果が真ではない | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:662 (canonicalTimestamp)` |
| M061 | 例外 | `-` | Unsupported revocation trigger | `REVOCATION_TRIGGERS` が input.trigger を含まない | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:475 (normalizeRegistration)` |
| M062 | 例外 | `-` | Unsupported cleanup scope | `REVOCATION_CLEANUP_SCOPES` が target.scope を含まない | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:561 (normalizeTargetReferences)` |
| M063 | 例外 | `-` | Cleanup discovery returned the wrong scope | `requiredScope` が存在し、真である、かつ `target.scope` が `requiredScope` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:562 (normalizeTargetReferences)` |
| M064 | 例外 | `-` | Revocation cleanup tenant identity is invalid | `value` が存在しない、または偽である、または `value.trim()` が `value` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-tenant-registry.ts:140 (canonicalTenantId)` |
| M065 | 例外 | `-` | Revocation cleanup tenant registry record is invalid | `record.schemaVersion` が `schemaVersion` と異なる、または `record.tenantId` が `tenantId` と異なる、または `record.tenantPartitionId` が `tenantPartitionId(tenantId)` と異なる、または `key` が `tenantRegistryKey(tenantId)` と異なる、または is finite の判定結果が真ではない、または is finite の判定結果が真ではない | `apps/api/src/rag/_shared/security/revocation-cleanup-tenant-registry.ts:136 (assertRecord)` |
| M066 | 例外 | `-` | Revocation cleanup manifest is invalid | `manifest.schemaVersion` が `1` と異なる、または `manifest.policyVersion` が `REVOCATION_CLEANUP_POLICY_VERSION` と異なる、または `manifest.tenantId` が `tenantId` と異なる、または `manifest.operationId` が `operationId` と異なる、または `canonicalIdentifier(manifest.tenantId, "tenantId")` が `manifest.tenantId` と異なる、または `canonicalIdentifier(manifest.operationId, "operationId")` が `manifest.operationId` と異なる、または `canonicalIdentifier(manifest.resourceId, "resourceId")` が `manifest.resourceId` と異なる、または `["document", "temporary_attachment", "folder", "resource_group", "benchmark_run", "account", "group"]` が manifest.resourceType を含まない、または `REVOCATION_TRIGGERS` が manifest.trigger を含まない、または `manifest.authoritativeDeny?.status` が `"effective"` と異なる、または `canonicalIdentifier(manifest.authoritativeDeny.version, "authoritativeDenyVersion")` が `manifest.authoritativeDeny.version` と異なる、または `canonicalTimestamp(manifest.authoritativeDeny.confirmedAt, "authoritativeDenyConfirmedAt")` が `manifest.authoritativeDeny.confirmedAt` と異なる、または `["cleanup_pending", "reconciliation_required", "completed", "superseded"]` が manifest.status を含まない、または is array の判定結果が真ではない、または some の判定結果が真である、または is safe integer の判定結果が真ではない、または `manifest.attempts` が `0` より小さい、または `canonicalTimestamp(manifest.createdAt, "createdAt")` が `manifest.createdAt` と異なる、または `canonicalTimestamp(manifest.updatedAt, "updatedAt")` が `manifest.updatedAt` と異なる、または is array の判定結果が真ではない、または is array の判定結果が真ではない、または `manifest.scopes.length` が `REVOCATION_CLEANUP_SCOPES.length` と異なる、または every の判定結果が真ではない | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:506 (validateManifest)` |
| M067 | 例外 | `-` | Revocation cleanup scope state is invalid | `REVOCATION_CLEANUP_SCOPES` が scope.scope を含まない、または `["pending", "verified"]` が scope.status を含まない | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:509 (validateManifest)` |
| M068 | 例外 | `-` | Revocation cleanup scope timestamp is invalid | `timestamp` が `undefined` と異なる、かつ `canonicalTimestamp(timestamp, "scopeTimestamp")` が `timestamp` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:513 (validateManifest)` |
| M069 | 例外 | `-` | Revocation cleanup target state is invalid | `REVOCATION_CLEANUP_SCOPES` が target.scope を含まない、または `canonicalIdentifier(target.reference, "cleanupTargetReference")` が `target.reference` と異なる、または `target.targetId` が `cleanupTargetId(target)` と異なる、または `["pending", "cleaned"]` が target.status を含まない、または is safe integer の判定結果が真ではない、または `target.attempts` が `0` より小さい、または has の判定結果が真である | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:527 (validateManifest)` |
| M070 | 例外 | `-` | Revocation cleanup target timestamp is invalid | `timestamp` が `undefined` と異なる、かつ `canonicalTimestamp(timestamp, "targetTimestamp")` が `timestamp` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:531 (validateManifest)` |
| M071 | 例外 | `-` | Revocation cleanup idempotency key was reused with different input | `manifest.resourceType` が `input.resourceType` と異なる、または `manifest.resourceId` が `input.resourceId` と異なる、または `manifest.trigger` が `input.trigger` と異なる、または `manifest.authoritativeDeny.version` が `input.authoritativeDenyVersion` と異なる、または `manifest.authoritativeDeny.confirmedAt` が `input.authoritativeDenyConfirmedAt` と異なる、または `JSON.stringify(manifest.deniedPurposes)` が `JSON.stringify(input.deniedPurposes)` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:549 (assertSameRegistration)` |
| M072 | 例外 | `-` | Revocation cleanup idempotency key was reused with different targets | some の判定結果が真である | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:552 (assertSameRegistration)` |
| M073 | 例外 | `-` | Invalid RAG observation timestamp | is finite の判定結果が真ではない | `apps/api/src/rag/quality-control/production-rag-observation-producer.ts:1113 (normalizeTimestamp)` |
| M074 | 例外 | `-` | Invalid RAG quality source sample identity | `sample.schemaVersion` が `1` と異なる、または `sample.signalCatalogVersion` が `RAG_QUALITY_SIGNAL_CATALOG_VERSION` と異なる、または trim の判定結果が真ではない、または trim の判定結果が真ではない、または trim の判定結果が真ではない、または trim の判定結果が真ではない、または trim の判定結果が真ではない、または `sample.sourceType` が存在しない、または偽である、または trim の判定結果が真ではない、または `sample.tenantPartitionId` が `undefined` と異なる、かつ test の判定結果が真ではない、または `sample.resourceIds` が `undefined` と異なる、かつ is array の判定結果が真ではない、または some の判定結果が真である、または `sample.securityResourceRefs` が `undefined` と異なる、かつ is array の判定結果が真ではない、または some の判定結果が真である、または trim の判定結果が真ではない、または is finite の判定結果が真ではない | `apps/api/src/rag/quality-control/production-rag-observation-producer.ts:1054 (assertSourceSample)` |
| M075 | 例外 | `-` | `Unknown RAG quality signal: ${signalId}` | `RAG_REQUIRED_SIGNAL_IDS` が signalId as RagQualitySignalId を含まない | `apps/api/src/rag/quality-control/production-rag-observation-producer.ts:1056 (assertSourceSample)` |
| M076 | 例外 | `-` | `Available RAG quality measurement is incomplete: ${signalId}` | is integer の判定結果が真ではない、または `item.sampleCount` が `0` より小さい、または `item.available` が存在し、真である、かつ `item.value` が `null` と等しい、または is finite の判定結果が真ではない、または `item.sampleCount` が `1` より小さい、または `item.confidence` が `null` と等しい、または `item.confidence` が `0` より小さい、または `item.confidence` が `1` より大きい | `apps/api/src/rag/quality-control/production-rag-observation-producer.ts:1069 (assertSourceSample)` |
| M077 | 例外 | `-` | `Unavailable RAG quality measurement must not contain a value: ${signalId}` | `item.available` が存在しない、または偽である、かつ `item.value` が `null` と異なる、または `item.confidence` が `null` と異なる | `apps/api/src/rag/quality-control/production-rag-observation-producer.ts:1072 (assertSourceSample)` |
| M078 | ログ | `-` | RAG quality source sample was not recorded | `result.skippedReason` が存在し、真である、かつ `process.env.RAG_MONITORING_REQUIRED` が `"1"` と等しい | `apps/api/src/rag/quality-control/production-rag-observation-producer.ts:828 (bestEffortCapture)` |
| M079 | ログ | `-` | RAG quality source sample persistence failed | 例外を捕捉した場合 | `apps/api/src/rag/quality-control/production-rag-observation-producer.ts:831 (bestEffortCapture)` |
| M080 | 例外 | `-` | Staged manifest failed publication validation | `manifest.documentId` が `run.artifactId` と異なる、または `stableHash(manifest.publicationFence)` が `stableHash(fence)` と異なる、または `fence.runId` が `run.runId` と異なる、または `fence.artifactId` が `run.artifactId` と異なる、または `fence.idempotencyKey` が `run.idempotencyKey` と異なる、または `fence.sourceId` が `run.scope.sourceId` と異なる、または `fence.purpose` が `run.scope.purpose` と異なる、または `manifest.lifecycleStatus` が `"staging"` と異なる、または `manifest.publicationEligible` が `true` と異なる、または `manifest.processingStatus` が `"complete"` と異なる、または `manifest.admission?.status` が `"approved"` と異なる、または `manifest.admission.tenantId` が `run.scope.tenantId` と異なる、または `manifest.derivedIntegrity?.verified` が `true` と異なる、または `manifest.derivedIntegrity.reasons.length` が `0` と異なる、または `manifest.documentVersion` が存在しない、または偽である、または is quality approved for normal rag の判定結果が真ではない | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:720 (StagedPublicationCoordinator.validateStagedManifest)` |
| M081 | 例外 | `-` | Staged manifest key does not match its fenced namespace | `manifest.manifestObjectKey` が `tenantDocumentArtifactKey( this.deps, run.scope.tenantId, `${fence.stageNamespace}/manifests/${run.artifactId}.json` )` と異なる | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:727 (StagedPublicationCoordinator.validateStagedManifest)` |
| M082 | 例外 | `-` | Staged manifest durable verification failed | `stableHash(persistedManifest)` が `stableHash(manifest)` と異なる | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:730 (StagedPublicationCoordinator.validateStagedManifest)` |
| M083 | 例外 | `-` | Staged object escaped its namespace | some の判定結果が真である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:732 (StagedPublicationCoordinator.validateStagedManifest)` |
| M084 | 例外 | `-` | Staged object content hash mismatch | `objectHashes?.source` が存在しない、または偽である、または `stableHash(objectByKey.get(manifest.sourceObjectKey))` が `objectHashes.source` と異なる、または optional object hash matches の判定結果が真ではない、または optional object hash matches の判定結果が真ではない | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:741 (StagedPublicationCoordinator.validateStagedManifest)` |
| M085 | 例外 | `-` | Staged manifest record inventory mismatch | `manifest.vectorKeys.length` が `new Set(manifest.vectorKeys).size` と異なる、または `stableHash(manifest.vectorKeys)` が `stableHash(expectedKeys)` と異なる、または `manifest.derivedIntegrity.evidenceRecordCount` が `evidenceKeys.length` と異なる、または `manifest.derivedIntegrity.memoryRecordCount` が `memoryKeys.length` と異なる、または `manifest.derivedIntegrity.expectedChunkCount` が `manifest.chunkCount` と異なる、または `manifest.derivedIntegrity.expectedMemoryCardCount` が `manifest.memoryCardCount` と異なる | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:752 (StagedPublicationCoordinator.validateStagedManifest)` |
| M086 | 例外 | `-` | Vector store does not support durable record verification | `this.deps.evidenceVectorStore.getByKeys` が存在しない、または偽である、または `this.deps.memoryVectorStore.getByKeys` が存在しない、または偽である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:1472 (StagedPublicationCoordinator.loadVectorRecords)` |
| M087 | 例外 | `-` | Staged vector record count mismatch | `records.length` が `manifest.vectorKeys.length` と異なる | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:754 (StagedPublicationCoordinator.validateStagedManifest)` |
| M088 | 例外 | `-` | Staged vector fencing metadata mismatch | some の判定結果が真である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:760 (StagedPublicationCoordinator.validateStagedManifest)` |
| M089 | 例外 | `-` | Staged admission references are incomplete | is complete approved admission の判定結果が真ではない | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:762 (StagedPublicationCoordinator.validateStagedManifest)` |
| M090 | 例外 | `-` | `Staged vector security envelope mismatch: ${[...new Set(envelopeReasons)].join(",")}` | `envelopeReasons.length` が `0` より大きい | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:768 (StagedPublicationCoordinator.validateStagedManifest)` |
| M091 | 例外 | `-` | Staged vector record hash mismatch | `derivedRecordSetHash(records)` が `manifest.derivedIntegrity.recordSetHash` と異なる | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:769 (StagedPublicationCoordinator.validateStagedManifest)` |
| M092 | 例外 | `-` | Publication run disappeared after checkpoint update | `nextStored` が存在しない、または偽である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:696 (StagedPublicationCoordinator.updateWithFence)` |
| M093 | 例外 | `-` | Publication run not found | `stored` が存在しない、または偽である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:583 (StagedPublicationCoordinator.getRun)` |
| M094 | 例外 | `-` | `Publication run is ${existing.status}` | `existing.status` が `"validated"` と異なる、かつ `existing.status` が `"committing"` と異なる | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:276 (StagedPublicationCoordinator.commit)` |
| M095 | 例外 | `-` | Publication run not found | `stored` が存在しない、または偽である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:373 (StagedPublicationCoordinator.reconcile)` |
| M096 | 例外 | `-` | Prepared rollback manifest security envelope verification failed | `sourceReasons.length` が `0` より大きい、または `chunkReasons.length` が `0` より大きい、または `(manifest.chunks ?? []).length` が `manifest.chunkCount` と異なる | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:1319 (StagedPublicationCoordinator.validatePreparedRollbackArtifact)` |
| M097 | 例外 | `-` | Rollback memory card ledger is invalid | is array の判定結果が真ではない | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:1727 (transformRollbackMemoryCards)` |
| M098 | 例外 | `-` | Prepared rollback memory security envelope verification failed | `verifyDerivedRecordSecurityEnvelope(card.securityEnvelope, input).length` が `0` より大きい | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:1731 (transformRollbackMemoryCards)` |
| M099 | 例外 | `-` | `Rollback source locator is missing: ${label}` | `locator` が存在しない、または偽である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:1748 (requiredSourceLocator)` |
| M100 | 例外 | `-` | Prepared rollback memory verification failed | `memoryCards.length` が `manifest.memoryCardCount` と異なる | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:1333 (StagedPublicationCoordinator.validatePreparedRollbackArtifact)` |
| M101 | 例外 | `-` | Prepared rollback artifact verification failed | `stableHash(sourceText)` が `manifest.derivedIntegrity?.objectHashes?.source` と異なる、または optional text hash matches の判定結果が真ではない、または optional text hash matches の判定結果が真ではない、または `records.length` が `manifest.vectorKeys.length` と異なる、または `evidenceRecords.length` が `prepared.evidenceVectorKeys.length` と異なる、または `memoryRecords.length` が `prepared.memoryVectorKeys.length` と異なる、または `stableHash(evidenceIds)` が `stableHash(manifestChunkIds)` と異なる、または `stableHash(memoryIds)` が `stableHash(ledgerMemoryIds)` と異なる、または `derivedRecordSetHash(records)` が `prepared.recordSetHash` と異なる、または `manifest.derivedIntegrity?.recordSetHash` が `prepared.recordSetHash` と異なる、または `manifest.derivedIntegrity.manifestHash` が `prepared.manifestHash` と異なる、または some の判定結果が真である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:1369 (StagedPublicationCoordinator.validatePreparedRollbackArtifact)` |
| M102 | 例外 | `-` | Prepared rollback manifest hash verification failed | `stableHash(projection)` が `prepared.manifestHash` と異なる | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:1383 (StagedPublicationCoordinator.validatePreparedRollbackArtifact)` |
| M103 | 例外 | `-` | Staged publication artifact is missing | `stagedArtifact` が存在しない、または偽である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:288 (StagedPublicationCoordinator.commit)` |
| M104 | 例外 | `-` | Staged manifest fencing metadata is missing | `stageFence` が存在しない、または偽である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:291 (StagedPublicationCoordinator.commit)` |
| M105 | 例外 | `-` | Published object copy verification failed | `stableHash(await this.deps.objectStore.getText(targetKey))` が `stableHash(source)` と異なる | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:1486 (StagedPublicationCoordinator.copyText)` |
| M106 | 例外 | `-` | Staged vector record identity is missing | `recordId` が存在しない、または偽である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:789 (StagedPublicationCoordinator.prepareCommitArtifacts)` |
| M107 | 例外 | `-` | Promoted vector verification failed | `persisted.length` が `promoted.length` と異なる、または `derivedRecordSetHash(persisted)` が `derivedRecordSetHash(promoted)` と異なる | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:811 (StagedPublicationCoordinator.prepareCommitArtifacts)` |
| M108 | 例外 | `-` | Canonical manifest verification failed | `stableHash(persistedManifest)` が `stableHash(manifest)` と異なる | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:863 (StagedPublicationCoordinator.prepareCommitArtifacts)` |
| M109 | 例外 | `-` | Active publication pointer is missing | `pointerStored` が存在しない、または偽である | `apps/api/src/rag/_shared/publication/staged-publication-coordinator.ts:323 (StagedPublicationCoordinator.commit)` |
| M110 | 例外 | `-` | Source governance identity changed | `record.schemaVersion` が `1` と異なる、または `record.sourceId` が `identity.sourceId` と異なる、または `record.tenantId` が `identity.tenantId` と異なる、または `record.ownerUserId` が `identity.ownerUserId` と異なる、または `isPublishedArtifact` が存在しない、または偽である、かつ `record.sourceVersion` が `identity.sourceVersion` と異なる | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:893 (assertRecordIdentity)` |
| M111 | 例外 | `-` | Source governance record was not persisted | `stored` が存在しない、または偽である | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:249 (SourceGovernanceApprovalService.ensureInitialRecord)` |
| M112 | 例外 | `-` | Source governance audit outbox is not configured | `auditOutbox` が存在しない、または偽である | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:268 (SourceGovernanceApprovalService.approve)` |
| M113 | 例外 | `-` | A previous source governance audit is still reconciling | `state.record.auditReconciliation` が存在し、真である | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:661 (SourceGovernanceApprovalService.assertPreviousAuditSettled)` |
| M114 | 例外 | `-` | Previous source governance audit status could not be verified | 例外を捕捉した場合 | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:669 (SourceGovernanceApprovalService.assertPreviousAuditSettled)` |
| M115 | 例外 | `-` | A previous source governance audit is still pending | `previous.status` が `"completed"` と異なる | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:672 (SourceGovernanceApprovalService.assertPreviousAuditSettled)` |
| M116 | 例外 | `-` | Source governance audit intent could not be persisted | 例外を捕捉した場合 | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:286 (SourceGovernanceApprovalService.approve)` |
| M117 | 例外 | `-` | expectedVersion is required and must be canonical | trim の判定結果が真ではない、または `input.expectedVersion.trim()` が `input.expectedVersion` と異なる | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:727 (validateApprovalInput)` |
| M118 | 例外 | `-` | reason is required and must be canonical | trim の判定結果が真ではない、または `input.reason.trim()` が `input.reason` と異なる | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:730 (validateApprovalInput)` |
| M119 | 例外 | `-` | classification level is invalid | `SOURCE_CLASSIFICATION_LEVELS` が input.classification.level を含まない | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:733 (validateApprovalInput)` |
| M120 | 例外 | `-` | All governance profile versions are required and must be canonical | trim の判定結果が真ではない、または `version.trim()` が `version` と異なる | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:742 (validateApprovalInput)` |
| M121 | 例外 | `-` | Source inspection must pass before approval | `input.inspection.status` が `"passed"` と異なる | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:746 (validateApprovalInput)` |
| M122 | 例外 | `-` | Usage policy must explicitly and uniquely allow normal_rag | `purposes.length` が `0` と等しい、または `purposes.length` が `new Set(purposes).size` と異なる、または some の判定結果が真である、または `purposes` が "normal_rag" を含まない | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:754 (validateApprovalInput)` |
| M123 | 例外 | `-` | Usage policy purpose flags are inconsistent | some の判定結果が真である | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:761 (validateApprovalInput)` |
| M124 | 例外 | `-` | Quality profile is not explicitly publishable | `quality.knowledgeQualityStatus` が `"approved"` と異なる、または `quality.verificationStatus` が `"verified"` と異なる、または `quality.freshnessStatus` が `"current"` と異なる、または `quality.supersessionStatus` が `"current"` と異なる、または `quality.extractionQualityStatus` が `"high"` と異なる、または `quality.ragEligibility` が `"eligible"` と異なる、または is array の判定結果が真ではない、または `quality.flags.length` が `0` と異なる、または `quality.confidence` が `undefined` と異なる、かつ is finite の判定結果が真ではない、または `quality.confidence` が `0` より小さい、または `quality.confidence` が `1` より大きい | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:774 (validateApprovalInput)` |
| M125 | 例外 | `-` | `Source governance state is ${state.record.status}` | `state.record.status` が `"unreviewed"` と異なる、かつ `state.record.status` が `"restricted"` と異なる、かつ `state.record.status` が `"reconciliation_required"` と異なる | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:299 (SourceGovernanceApprovalService.approve)` |
| M126 | 例外 | `-` | A previous source governance audit is still reconciling | `state.record.auditReconciliation` が存在し、真である | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:302 (SourceGovernanceApprovalService.approve)` |
| M127 | 例外 | `-` | `${name} is missing or non-canonical` | `value` が存在しない、または偽である、または `value.trim()` が `value` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:272 (assertIdentifier)` |
| M128 | 例外 | `-` | Revocation cleanup resource fence listing escaped its resource partition | starts with の判定結果が真ではない、または ends with の判定結果が真ではない | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:111 (ObjectStoreRevocationCleanupRepairOutbox.assertResourceFenceReleased)` |
| M129 | 例外 | `-` | Revocation cleanup repair intent is invalid | `intent.schemaVersion` が `REVOCATION_CLEANUP_REPAIR_SCHEMA_VERSION` と異なる、または `["prepared", "deny_committed", "cleanup_registered", "cleanup_completed", "abandoned"]` が intent.status を含まない、または `intent.operationId` が `intent.cleanupRegistration.operationId` と異なる、または `intent.tenantId` が `intent.cleanupRegistration.tenantId` と異なる、または `intent.resourceType` が `intent.cleanupRegistration.resourceType` と異なる、または `intent.resourceId` が `intent.cleanupRegistration.resourceId` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:237 (validateStored)` |
| M130 | 例外 | `-` | `${name} is invalid` | is finite の判定結果が真ではない、または `new Date(value).toISOString()` が `value` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:277 (assertTimestamp)` |
| M131 | 例外 | `-` | Listed revocation cleanup resource fence disappeared | `stored` が存在しない、または偽である | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:113 (ObjectStoreRevocationCleanupRepairOutbox.assertResourceFenceReleased)` |
| M132 | 例外 | `-` | Revocation cleanup repair key is not canonical | `key` が `repairKey(intent.tenantId, intent.resourceType, intent.resourceId, intent.operationId)` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:255 (assertCanonicalKey)` |
| M133 | 例外 | `-` | Source revocation cleanup is still fenced | 例外を捕捉した場合 | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:312 (SourceGovernanceApprovalService.approve)` |
| M134 | 例外 | `-` | Current actor identity could not be verified | 例外を捕捉した場合 | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:551 (SourceGovernanceApprovalService.resolveCurrentActor)` |
| M135 | 例外 | `-` | Authoritative current actor identity is not configured | `this.deps.allowSnapshotActor` が存在しない、または偽である、または is active account の判定結果が真ではない、または trim の判定結果が真ではない、または trim の判定結果が真ではない | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:566 (SourceGovernanceApprovalService.resolveCurrentActor)` |
| M136 | 例外 | `-` | Source governance state could not be persisted | 例外を捕捉した場合 | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:608 (SourceGovernanceApprovalService.transition)` |
| M137 | 例外 | `-` | Source governance state verification failed | `stored` が存在しない、または偽である、または `stored.record.revision` が `next.revision` と異なる、または `stored.record.auditIntentId` が `next.auditIntentId` と異なる | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:612 (SourceGovernanceApprovalService.transition)` |
| M138 | 例外 | `-` | Approved governance policy is missing | `approval` が存在しない、または偽である | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:1025 (governancePublicationSourceVersion)` |
| M139 | 例外 | `-` | Staged candidate did not preserve the approved governance profile | `staged.runId` が存在しない、または偽である、または `candidate.publicationFence?.runId` が `staged.runId` と異なる、または `candidate.lifecycleStatus` が `"staging"` と異なる、または `candidate.processingStatus` が `"complete"` と異なる、または `candidate.publicationEligible` が `true` と異なる、または `candidate.admission?.status` が `"approved"` と異なる、または `candidate.admission.inspectionStatus` が `"passed"` と異なる、または `candidate.admission.tenantId` が `record.tenantId` と異なる、または `candidate.admission.classificationRef?.hash` が `approval.classificationRef.hash` と異なる、または `candidate.admission.usagePolicyRef?.hash` が `approval.usagePolicyRef.hash` と異なる、または `candidate.admission.qualityRef?.hash` が `approval.qualityRef.hash` と異なる、または `candidate.derivedIntegrity?.verified` が `true` と異なる | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:858 (assertApprovedStagedCandidate)` |
| M140 | 例外 | `-` | Published source identity does not match the approved candidate | `committed.activeDocumentId` が `staged.candidate.documentId` と異なる | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:359 (SourceGovernanceApprovalService.approve)` |
| M141 | 例外 | `-` | Source governance failure audit is pending reconciliation | 例外を捕捉した場合 | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:385 (SourceGovernanceApprovalService.approve)` |
| M142 | 例外 | `-` | Published source audit is pending reconciliation | 例外を捕捉した場合 | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:401 (SourceGovernanceApprovalService.approve)` |

## 読み方

- `contract` は API contract 上の応答説明であり、実装 literal と同一とは限りません。
- `http-response` / `exception` / `event` / `log` は実コードに存在する literal を根拠にしています。
- 動的に組み立てる文字列は式として確定できる場合だけ掲載し、推測した文面は補いません。
