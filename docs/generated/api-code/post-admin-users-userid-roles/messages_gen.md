<!-- This file is generated by npm run docs:api-code. Do not edit manually. -->

# POST /admin/users/{userId}/roles メッセージ仕様

## 生成根拠

- OpenAPI response description
- handler と到達関数の `c.json({ error/message })`
- `HTTPException` / `Error` の literal message
- logger call と SSE event の literal message

## メッセージ一覧

| ID | 種別 | Status/Event | メッセージ | 発生条件 | 実装根拠 |
| --- | --- | --- | --- | --- | --- |
| M001 | OpenAPI contract | `200` | リクエストは成功し、レスポンス body に結果を返します。 | OpenAPI で宣言された HTTP 200 response | runtime OpenAPI |
| M002 | OpenAPI contract | `401` | 認証が必要です。 | OpenAPI で宣言された HTTP 401 response | runtime OpenAPI |
| M003 | OpenAPI contract | `403` | 対象操作を実行する権限がありません。 | OpenAPI で宣言された HTTP 403 response | runtime OpenAPI |
| M004 | OpenAPI contract | `404` | 指定したリソースが見つかりません。 | OpenAPI で宣言された HTTP 404 response | runtime OpenAPI |
| M005 | OpenAPI contract | `503` | 正本 role の更新または session 失効に失敗しました | OpenAPI で宣言された HTTP 503 response | runtime OpenAPI |
| M006 | 例外 | `403` | Forbidden | 利用者が 指定された permission を持たない | `apps/api/src/authorization.ts:184 (requirePermission)` |
| M007 | 例外 | `-` | Authoritative directory identity is unavailable during reconciliation | `this.deps.verifiedIdentityProvider` が存在し、真である、かつ `currentIdentity` が存在しない、または偽である | `apps/api/src/rag/memorag-service.ts:3512 (MemoRagService.syncUserDirectory)` |
| M008 | 例外 | `-` | Admin ledger storage identity is missing | `db._storageKey` が存在しない、または偽である | `apps/api/src/rag/memorag-service.ts:3553 (MemoRagService.saveAdminLedger)` |
| M009 | 例外 | `-` | Admin ledger concurrent write conflict | `(error as { code?: string }).code` が `"PRECONDITION_FAILED"` と等しい | `apps/api/src/rag/memorag-service.ts:3565 (MemoRagService.saveAdminLedger)` |
| M010 | 例外 | `-` | Admin ledger concurrent write conflict | `committed.text` が `serialized` と異なる | `apps/api/src/rag/memorag-service.ts:3570 (MemoRagService.saveAdminLedger)` |
| M011 | 例外 | `-` | Authoritative identity lookup failed | 例外を捕捉した場合 | `apps/api/src/security/application-role-mutation-service.ts:337 (ApplicationRoleMutationService.resolveIdentity)` |
| M012 | 例外 | `-` | Authoritative actor tenant is missing | `tenantId` が存在しない、または偽である | `apps/api/src/security/application-role-mutation-service.ts:506 (ApplicationRoleMutationService.recordEarlyFailure)` |
| M013 | 例外 | `-` | Actor or target principal is not authoritative | `actorIdentity` が存在しない、または偽である、または `target` が存在しない、または偽である | `apps/api/src/security/application-role-mutation-service.ts:97 (ApplicationRoleMutationService.replaceRoles)` |
| M014 | 例外 | `-` | Role set contains an unknown or missing application role | `values.length` が `0` と等しい、または some の判定結果が真である | `apps/api/src/security/application-role-mutation-service.ts:567 (canonicalRoles)` |
| M015 | 例外 | `-` | Role set contains duplicates | `new Set(values).size` が `values.length` と異なる | `apps/api/src/security/application-role-mutation-service.ts:570 (canonicalRoles)` |
| M016 | 例外 | `-` | Role mutation reason is required and must be canonical | `value` が存在しない、または偽である、または `value.trim()` が `value` と異なる | `apps/api/src/security/application-role-mutation-service.ts:556 (canonicalReason)` |
| M017 | 例外 | `-` | Actor and target must be active | `actor.accountStatus` が `"active"` と異なる、または `target.accountStatus` が `"active"` と異なる | `apps/api/src/security/application-role-mutation-service.ts:282 (ApplicationRoleMutationService.validate)` |
| M018 | 例外 | `-` | Actor and target must belong to the same authoritative tenant | `actor.tenantId` が `target.tenantId` と異なる、または `actorSnapshot.tenantId` が `actor.tenantId` と異なる、または `actorSnapshot.userId` が `actor.userId` と異なる | `apps/api/src/security/application-role-mutation-service.ts:288 (ApplicationRoleMutationService.validate)` |
| M019 | 例外 | `-` | Actor lacks role mutation permission | 利用者が "access:role:assign" permission を持たない | `apps/api/src/security/application-role-mutation-service.ts:299 (ApplicationRoleMutationService.validate)` |
| M020 | 例外 | `-` | Self role mutation is forbidden | `actor.userId` が `target.userId` と等しい | `apps/api/src/security/application-role-mutation-service.ts:304 (ApplicationRoleMutationService.validate)` |
| M021 | 例外 | `-` | Only a system administrator can grant system recovery authority | `afterRoles` が "SYSTEM_ADMIN" を含む、かつ `actor.cognitoGroups` が "SYSTEM_ADMIN" を含まない | `apps/api/src/security/application-role-mutation-service.ts:307 (ApplicationRoleMutationService.validate)` |
| M022 | 例外 | `-` | Authoritative recovery-principal lookup failed | `current` が存在しない、または偽である | `apps/api/src/security/application-role-mutation-service.ts:319 (ApplicationRoleMutationService.validate)` |
| M023 | 例外 | `-` | The last administrative recovery principal cannot be removed | `otherRecoveryPrincipals.length` が `0` と等しい | `apps/api/src/security/application-role-mutation-service.ts:328 (ApplicationRoleMutationService.validate)` |
| M024 | 例外 | `-` | `Application-role mutation lock ${field} is missing or non-canonical` | `normalized` が存在しない、または偽である、または `normalized` が `value` と異なる | `apps/api/src/security/application-role-mutation-lock.ts:353 (canonical)` |
| M025 | 例外 | `-` | `Application-role mutation lock ${field} is invalid` | `values.length` が `0` と等しい、または some の判定結果が真である、または `new Set(values).size` が `values.length` と異なる | `apps/api/src/security/application-role-mutation-lock.ts:292 (canonicalRoles)` |
| M026 | 例外 | `-` | Authoritative tenant is required | `normalized` が存在しない、または偽である | `apps/api/src/security/tenant-partition.ts:5 (tenantPartitionId)` |
| M027 | 例外 | `-` | Application-role mutation lock record is invalid | `record.schemaVersion` が `APPLICATION_ROLE_MUTATION_LOCK_SCHEMA_VERSION` と異なる、または `record.tenantId` が `tenantId` と異なる、または `record.operationId` が存在しない、または偽である、または `record.auditIntentId` が存在しない、または偽である、または `record.fencingToken` が存在しない、または偽である、または `record.targetUserId` が存在しない、または偽である、または `record.targetUsername` が存在しない、または偽である、または is safe integer の判定結果が真ではない、または `record.revision` が `1` より小さい、または is finite の判定結果が真ではない、または `["held", "identity_mutation", "managed_commit", "managed_committed", "recovering", "released"]` が record.phase を含まない、または `record.expectedRoles.length` が `0` と等しい、または `record.desiredRoles.length` が `0` と等しい、または `new Set(record.expectedRoles).size` が `record.expectedRoles.length` と異なる、または `new Set(record.desiredRoles).size` が `record.desiredRoles.length` と異なる、または `record.recoveryAction` が `undefined` と異なる、かつ `record.recoveryAction` が `"rollback_identity"` と異なる、かつ `record.recoveryAction` が `"complete_managed"` と異なる、かつ `record.recoveryAction` が `"reconcile_managed"` と異なる、または some の判定結果が真である、または some の判定結果が真である、または same values の判定結果が真ではない、または same values の判定結果が真ではない | `apps/api/src/security/application-role-mutation-lock.ts:336 (assertRecord)` |
| M028 | 例外 | `-` | Application-role operation id was reused with different mutation data | `current.auditIntentId` が `input.auditIntentId` と異なる、または `current.targetUserId` が `input.targetUserId` と異なる、または `current.targetUsername` が `input.targetUsername` と異なる、または same values の判定結果が真ではない、または same values の判定結果が真ではない | `apps/api/src/security/application-role-mutation-lock.ts:308 (assertSameMutation)` |
| M029 | 例外 | `-` | Application-role mutation lock acquisition did not converge | 当該処理へ到達した場合 | `apps/api/src/security/application-role-mutation-lock.ts:123 (ObjectStoreApplicationRoleMutationLock.acquire)` |
| M030 | 例外 | `-` | Application-role mutation no longer requires identity recovery | `current.record.phase` が `"released"` と等しい、または `current.record.phase` が `"held"` と等しい | `apps/api/src/security/application-role-mutation-lock.ts:171 (ObjectStoreApplicationRoleMutationLock.beginExpiredRecovery)` |
| M031 | 例外 | `-` | Application-role mutation recovery lease is still active | is expired の判定結果が真ではない | `apps/api/src/security/application-role-mutation-lock.ts:174 (ObjectStoreApplicationRoleMutationLock.beginExpiredRecovery)` |
| M032 | 例外 | `-` | Application-role recovery CAS race was lost | is conditional write error の判定結果が真である | `apps/api/src/security/application-role-mutation-lock.ts:195 (ObjectStoreApplicationRoleMutationLock.beginExpiredRecovery)` |
| M033 | 例外 | `-` | Expired role mutation target cannot be reconciled authoritatively | `target` が存在しない、または偽である、または `target.tenantId` が `record.tenantId` と異なる、または `target.username` が `record.targetUsername` と異なる | `apps/api/src/security/application-role-mutation-service.ts:377 (ApplicationRoleMutationService.recoverExpiredMutation)` |
| M034 | 例外 | `-` | Expired role mutation stopped during the managed-state commit boundary | `record.recoveryAction` が `"reconcile_managed"` と等しい | `apps/api/src/security/application-role-mutation-service.ts:381 (ApplicationRoleMutationService.recoverExpiredMutation)` |
| M035 | 例外 | `-` | Managed-committed role mutation does not match authoritative identity | same role values の判定結果が真ではない | `apps/api/src/security/application-role-mutation-service.ts:386 (ApplicationRoleMutationService.recoverExpiredMutation)` |
| M036 | 例外 | `-` | Expired role mutation recovery adapter is not configured | `this.deps.userDirectory.revokeSessions` が存在しない、または偽である、または `this.deps.userDirectory.replaceApplicationRoles` が存在しない、または偽である | `apps/api/src/security/application-role-mutation-service.ts:400 (ApplicationRoleMutationService.recoverExpiredMutation)` |
| M037 | 例外 | `-` | Expired role mutation rollback verification failed | `recovered` が存在しない、または偽である、または same roles の判定結果が真ではない | `apps/api/src/security/application-role-mutation-service.ts:418 (ApplicationRoleMutationService.recoverExpiredMutation)` |
| M038 | 例外 | `-` | `${name} is missing or non-canonical` | `value` が存在しない、または偽である、または `value.trim()` が `value` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:272 (assertIdentifier)` |
| M039 | 例外 | `-` | Revocation cleanup repair intent is invalid | `intent.schemaVersion` が `REVOCATION_CLEANUP_REPAIR_SCHEMA_VERSION` と異なる、または `["prepared", "deny_committed", "cleanup_registered", "cleanup_completed", "abandoned"]` が intent.status を含まない、または `intent.operationId` が `intent.cleanupRegistration.operationId` と異なる、または `intent.tenantId` が `intent.cleanupRegistration.tenantId` と異なる、または `intent.resourceType` が `intent.cleanupRegistration.resourceType` と異なる、または `intent.resourceId` が `intent.cleanupRegistration.resourceId` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:237 (validateStored)` |
| M040 | 例外 | `-` | `${name} is invalid` | is finite の判定結果が真ではない、または `new Date(value).toISOString()` が `value` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:277 (assertTimestamp)` |
| M041 | 例外 | `-` | Revocation cleanup repair intent was not found | `stored` が存在しない、または偽である | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:146 (ObjectStoreRevocationCleanupRepairOutbox.transition)` |
| M042 | 例外 | `-` | Revocation cleanup repair intent identity mismatch | `intent.tenantId` が `identity.tenantId` と異なる、または `intent.resourceType` が `identity.resourceType` と異なる、または `intent.resourceId` が `identity.resourceId` と異なる、または `intent.operationId` が `identity.operationId` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:268 (assertIdentity)` |
| M043 | 例外 | `-` | Revocation cleanup repair transition is invalid | transition allowed の判定結果が真ではない | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:149 (ObjectStoreRevocationCleanupRepairOutbox.transition)` |
| M044 | 例外 | `-` | Revocation cleanup repair transition did not converge | 当該処理へ到達した場合 | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:158 (ObjectStoreRevocationCleanupRepairOutbox.transition)` |
| M045 | 例外 | `-` | Expired application-role mutation recovery did not converge | 当該処理へ到達した場合 | `apps/api/src/security/application-role-mutation-service.ts:366 (ApplicationRoleMutationService.acquireWithExpiredRecovery)` |
| M046 | 例外 | `-` | Actor or target principal disappeared while acquiring the mutation fence | `lockedActor` が存在しない、または偽である、または `lockedTarget` が存在しない、または偽である | `apps/api/src/security/application-role-mutation-service.ts:152 (ApplicationRoleMutationService.replaceRoles)` |
| M047 | 例外 | `-` | Authoritative target roles changed before the fenced mutation | `lockedTarget.tenantId` が `target.tenantId` と異なる、または `lockedTarget.username` が `target.username` と異なる、または same role values の判定結果が真ではない | `apps/api/src/security/application-role-mutation-service.ts:159 (ApplicationRoleMutationService.replaceRoles)` |
| M048 | 例外 | `-` | Authoritative fenced role mutation adapter is not configured | `this.deps.userDirectory.revokeSessions` が存在しない、または偽である、または `this.deps.userDirectory.replaceApplicationRoles` が存在しない、または偽である | `apps/api/src/security/application-role-mutation-service.ts:173 (ApplicationRoleMutationService.replaceRoles)` |
| M049 | 例外 | `-` | Revocation cleanup resource fence listing escaped its resource partition | starts with の判定結果が真ではない、または ends with の判定結果が真ではない | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:111 (ObjectStoreRevocationCleanupRepairOutbox.assertResourceFenceReleased)` |
| M050 | 例外 | `-` | Listed revocation cleanup resource fence disappeared | `stored` が存在しない、または偽である | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:113 (ObjectStoreRevocationCleanupRepairOutbox.assertResourceFenceReleased)` |
| M051 | 例外 | `-` | Revocation cleanup repair key is not canonical | `key` が `repairKey(intent.tenantId, intent.resourceType, intent.resourceId, intent.operationId)` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:255 (assertCanonicalKey)` |
| M052 | 例外 | `-` | Revocation cleanup tenant identity is invalid | `value` が存在しない、または偽である、または `value.trim()` が `value` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-tenant-registry.ts:140 (canonicalTenantId)` |
| M053 | 例外 | `-` | Revocation cleanup tenant registry record is invalid | `record.schemaVersion` が `schemaVersion` と異なる、または `record.tenantId` が `tenantId` と異なる、または `record.tenantPartitionId` が `tenantPartitionId(tenantId)` と異なる、または `key` が `tenantRegistryKey(tenantId)` と異なる、または is finite の判定結果が真ではない、または is finite の判定結果が真ではない | `apps/api/src/rag/_shared/security/revocation-cleanup-tenant-registry.ts:136 (assertRecord)` |
| M054 | 例外 | `-` | Revocation cleanup repair intent conflicts with an existing operation | `intent.expectedBeforeDenyVersion` が `input.expectedBeforeDenyVersion` と異なる、または `JSON.stringify(intent.cleanupRegistration)` が `JSON.stringify(input.cleanupRegistration)` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-repair-outbox.ts:250 (assertSameIntent)` |
| M055 | 例外 | `-` | `Application-role mutation phase changed to ${current.record.phase}` | `allowedPhases` が current.record.phase を含まない | `apps/api/src/security/application-role-mutation-lock.ts:209 (ObjectStoreApplicationRoleMutationLock.transition)` |
| M056 | 例外 | `-` | Authoritative role set verification failed | `verifiedAfter` が存在しない、または偽である、または same roles の判定結果が真ではない | `apps/api/src/security/application-role-mutation-service.ts:226 (ApplicationRoleMutationService.replaceRoles)` |
| M057 | 例外 | `-` | `${field} is invalid` | `normalized` が存在しない、または偽である、または `normalized.length` が `512` より大きい、または `hasControlCharacter` が存在し、真である | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:651 (canonicalIdentifier)` |
| M058 | 例外 | `-` | `${field} is invalid` | is finite の判定結果が真ではない | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:662 (canonicalTimestamp)` |
| M059 | 例外 | `-` | Unsupported revocation trigger | `REVOCATION_TRIGGERS` が input.trigger を含まない | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:475 (normalizeRegistration)` |
| M060 | 例外 | `-` | Unsupported cleanup scope | `REVOCATION_CLEANUP_SCOPES` が target.scope を含まない | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:561 (normalizeTargetReferences)` |
| M061 | 例外 | `-` | Cleanup discovery returned the wrong scope | `requiredScope` が存在し、真である、かつ `target.scope` が `requiredScope` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:562 (normalizeTargetReferences)` |
| M062 | 例外 | `-` | Revocation cleanup manifest is invalid | `manifest.schemaVersion` が `1` と異なる、または `manifest.policyVersion` が `REVOCATION_CLEANUP_POLICY_VERSION` と異なる、または `manifest.tenantId` が `tenantId` と異なる、または `manifest.operationId` が `operationId` と異なる、または `canonicalIdentifier(manifest.tenantId, "tenantId")` が `manifest.tenantId` と異なる、または `canonicalIdentifier(manifest.operationId, "operationId")` が `manifest.operationId` と異なる、または `canonicalIdentifier(manifest.resourceId, "resourceId")` が `manifest.resourceId` と異なる、または `["document", "temporary_attachment", "folder", "resource_group", "benchmark_run", "account", "group"]` が manifest.resourceType を含まない、または `REVOCATION_TRIGGERS` が manifest.trigger を含まない、または `manifest.authoritativeDeny?.status` が `"effective"` と異なる、または `canonicalIdentifier(manifest.authoritativeDeny.version, "authoritativeDenyVersion")` が `manifest.authoritativeDeny.version` と異なる、または `canonicalTimestamp(manifest.authoritativeDeny.confirmedAt, "authoritativeDenyConfirmedAt")` が `manifest.authoritativeDeny.confirmedAt` と異なる、または `["cleanup_pending", "reconciliation_required", "completed", "superseded"]` が manifest.status を含まない、または is array の判定結果が真ではない、または some の判定結果が真である、または is safe integer の判定結果が真ではない、または `manifest.attempts` が `0` より小さい、または `canonicalTimestamp(manifest.createdAt, "createdAt")` が `manifest.createdAt` と異なる、または `canonicalTimestamp(manifest.updatedAt, "updatedAt")` が `manifest.updatedAt` と異なる、または is array の判定結果が真ではない、または is array の判定結果が真ではない、または `manifest.scopes.length` が `REVOCATION_CLEANUP_SCOPES.length` と異なる、または every の判定結果が真ではない | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:506 (validateManifest)` |
| M063 | 例外 | `-` | Revocation cleanup scope state is invalid | `REVOCATION_CLEANUP_SCOPES` が scope.scope を含まない、または `["pending", "verified"]` が scope.status を含まない | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:509 (validateManifest)` |
| M064 | 例外 | `-` | Revocation cleanup scope timestamp is invalid | `timestamp` が `undefined` と異なる、かつ `canonicalTimestamp(timestamp, "scopeTimestamp")` が `timestamp` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:513 (validateManifest)` |
| M065 | 例外 | `-` | Revocation cleanup target state is invalid | `REVOCATION_CLEANUP_SCOPES` が target.scope を含まない、または `canonicalIdentifier(target.reference, "cleanupTargetReference")` が `target.reference` と異なる、または `target.targetId` が `cleanupTargetId(target)` と異なる、または `["pending", "cleaned"]` が target.status を含まない、または is safe integer の判定結果が真ではない、または `target.attempts` が `0` より小さい、または has の判定結果が真である | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:527 (validateManifest)` |
| M066 | 例外 | `-` | Revocation cleanup target timestamp is invalid | `timestamp` が `undefined` と異なる、かつ `canonicalTimestamp(timestamp, "targetTimestamp")` が `timestamp` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:531 (validateManifest)` |
| M067 | 例外 | `-` | Revocation cleanup idempotency key was reused with different input | `manifest.resourceType` が `input.resourceType` と異なる、または `manifest.resourceId` が `input.resourceId` と異なる、または `manifest.trigger` が `input.trigger` と異なる、または `manifest.authoritativeDeny.version` が `input.authoritativeDenyVersion` と異なる、または `manifest.authoritativeDeny.confirmedAt` が `input.authoritativeDenyConfirmedAt` と異なる、または `JSON.stringify(manifest.deniedPurposes)` が `JSON.stringify(input.deniedPurposes)` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:549 (assertSameRegistration)` |
| M068 | 例外 | `-` | Revocation cleanup idempotency key was reused with different targets | some の判定結果が真である | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:552 (assertSameRegistration)` |
| M069 | 例外 | `-` | Authoritative role mutation is not configured | `config.authEnabled` が存在し、真である | `apps/api/src/rag/memorag-service.ts:2136 (MemoRagService.assignUserRoles)` |
| M070 | HTTP 実装応答 | `404` | User not found | `user` が存在しない、または偽である | `apps/api/src/routes/admin-routes.ts:269 (POST /admin/users/{userId}/roles handler)` |

## 読み方

- `contract` は API contract 上の応答説明であり、実装 literal と同一とは限りません。
- `http-response` / `exception` / `event` / `log` は実コードに存在する literal を根拠にしています。
- 動的に組み立てる文字列は式として確定できる場合だけ掲載し、推測した文面は補いません。
