<!-- This file is generated by npm run docs:api-code. Do not edit manually. -->

# POST /favorites 詳細設計

## 1. API の役割

お気に入り shortcut を作成する

権限再確認 resolver が実装済みの会話、文書、フォルダを shortcut として保存します。対象本体は作成しません。

| 項目 | 内容 |
| --- | --- |
| Method | `POST` |
| Path | `/favorites` |
| OpenAPI contract | あり（runtime `/openapi.json`） |
| Route 定義 | `apps/api/src/routes/favorite-routes.ts:25 (POST /favorites)` |
| Handler | `apps/api/src/routes/favorite-routes.ts:41 (POST /favorites handler)` |

## 2. 主処理

| # | 深さ | 種別 | 自然言語での処理 | コード根拠 | 実装位置 |
| ---: | ---: | --- | --- | --- | --- |
| 1 | 0 | 認証・認可 | 認証済み利用者を request context から取得する。 | `user = c.get("user")` | `apps/api/src/routes/favorite-routes.ts:42` |
| 2 | 0 | 認証・認可 | "chat:create" permission を必須条件として確認する。 | `requirePermission(user, "chat:create")` | `apps/api/src/routes/favorite-routes.ts:43` |
| 3 | 0 | 入力検証 | schema 検証済みの JSON request body を取得する。 | `body = validJson<z.infer<typeof CreateFavoriteRequestSchema>>(c)` | `apps/api/src/routes/favorite-routes.ts:44` |
| 4 | 0 | 応答 | 処理結果を HTTP 200 の JSON response として返す。 | `return c.json(FavoriteSchema.parse(await service.saveFavorite(user, body)), 200)` | `apps/api/src/routes/favorite-routes.ts:45` |

## 3. 条件分岐・例外

| # | 所属関数 | 種別 | 条件の意味 | 根拠式 | 実装位置 |
| ---: | --- | --- | --- | --- | --- |
| B001 | `requirePermission` | if | 利用者が 指定された permission を持たない | `!hasPermission(user, permission)` | `apps/api/src/authorization.ts:184 (requirePermission)` |

## 4. 到達する主要実装

handler を起点に TypeScript symbol を解決し、深さ 2 までの主要関数・method を列挙しています。深い helper を含む全到達関数は 13 件で、永続化・外部接続は深さにかかわらず次節へ集約しています。

| 深さ | Symbol | 責務 | 実装位置 |
| ---: | --- | --- | --- |
| 0 | `POST /favorites handler` | POST /favorites の request を受け、検証・認可・service 呼び出し・HTTP 応答を調整する。 | `apps/api/src/routes/favorite-routes.ts:41 (POST /favorites handler)` |
| 1 | `requirePermission` | require permission の実装処理を担当する。 | `apps/api/src/authorization.ts:183 (requirePermission)` |
| 2 | `hasPermission` | has permission の実装処理を担当する。 | `apps/api/src/authorization.ts:187 (hasPermission)` |
| 1 | `validJson` | valid json の実装処理を担当する。 | `apps/api/src/routes/route-utils.ts:20 (validJson)` |
| 2 | `validRequest` | valid request の実装処理を担当する。 | `apps/api/src/routes/route-utils.ts:36 (validRequest)` |
| 1 | `MemoRagService.saveFavorite` | save favorite の実装処理を担当する。 | `apps/api/src/rag/memorag-service.ts:4210 (MemoRagService.saveFavorite)` |
| 2 | `FavoriteService.save` | save の実装処理を担当する。 | `apps/api/src/favorites/favorite-service.ts:28 (FavoriteService.save)` |

## 5. データ・外部境界

| 種別 | 境界 | Target | Operation | 目的 | Caller | 実装位置 |
| --- | --- | --- | --- | --- | --- | --- |
| 作成・追記 | Store | `this.ports.favoriteStore` | `save` | `this.ports.favoriteStore` に対して save を実行する。 | `FavoriteService.save` | `apps/api/src/favorites/favorite-service.ts:32 (FavoriteService.save)` |
| 参照 | Store | `this.ports.conversationHistoryStore` | `list` | `this.ports.conversationHistoryStore` に対して list を実行する。 | `FavoriteService.resolveVisibility` | `apps/api/src/favorites/favorite-service.ts:69 (FavoriteService.resolveVisibility)` |

## 6. 応答・メッセージ

| 種別 | Status/Event | 内容 | 発生条件 |
| --- | --- | --- | --- |
| OpenAPI contract | `200` | リクエストは成功し、レスポンス body に結果を返します。 | OpenAPI で宣言された HTTP 200 response |
| OpenAPI contract | `400` | リクエスト形式または入力値が不正です。 | OpenAPI で宣言された HTTP 400 response |
| OpenAPI contract | `401` | 認証が必要です。 | OpenAPI で宣言された HTTP 401 response |
| OpenAPI contract | `403` | 対象操作を実行する権限がありません。 | OpenAPI で宣言された HTTP 403 response |
| 例外 | `403` | Forbidden | 利用者が 指定された permission を持たない |
| 例外 | `-` | `Unsupported favorite target type: ${input.targetType}` | favorite target resolver implemented の判定結果が真ではない |

## 7. テスト対応

| 関連 | Test case | 実装位置 |
| --- | --- | --- |
| 到達 symbol | favoriteCreateDoesNotReturnAccessibleTrueWithoutResolver | `apps/api/src/rag/memorag-service.test.ts:1719 (favoriteCreateDoesNotReturnAccessibleTrueWithoutResolver)` |
| 到達 symbol | favoriteCreateRechecksChatSessionOwner | `apps/api/src/rag/memorag-service.test.ts:1726 (favoriteCreateRechecksChatSessionOwner)` |

## 8. 解析上の注意

- 既知の operation 固有 warning はありません。

- 記載内容は route、runtime OpenAPI、handler から到達する repository 内 symbol、既存 test を解析した派生成果物です。
- 可読性のため、詳細設計と unit test の分岐は handler と直下関数（深さ 1 まで）へ絞り、深い共通 helper の内部分岐は API 固有要件として重複掲載しません。
- 文書生成専用 annotation、decorator、コメント、API 別設定表は入力にしていません。
- interface / dynamic dispatch の実装先を TypeScript checker が一意に解決できない場合、logical store call までを確定情報として記載します。
