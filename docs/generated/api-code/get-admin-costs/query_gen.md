<!-- This file is generated by npm run docs:api-code. Do not edit manually. -->

# GET /admin/costs Query・永続化仕様

このアプリケーションは API operation ごとの SQL ファイルを持たないため、handler から到達する store / object / vector / external port の実呼び出しを query 単位として記載します。

## Store・永続化操作

| # | CRUD/実行 | Target | Method | 自然言語での目的 | Caller | コード根拠 |
| ---: | --- | --- | --- | --- | --- | --- |
| 1 | 参照 | `this.deps.usageEventStore` | `query` | `this.deps.usageEventStore` に対して query を実行する。 | `MemoRagService.listUsageSummaries` | `apps/api/src/rag/memorag-service.ts:2243 (MemoRagService.listUsageSummaries)` |

## 外部サービス操作

| # | CRUD/実行 | Target | Method | 自然言語での目的 | Caller | コード根拠 |
| ---: | --- | --- | --- | --- | --- | --- |
| 1 | 実行 | `catalog<br>    .filter((entry) => entry.provider === event.provider)<br>    ` | `filter` | `catalog<br>    .filter((entry) => entry.provider === event.provider)<br>    ` へ filter を実行する。 | `findPrice` | `apps/api/src/rag/_shared/usage/usage-pricing-catalog.ts:69 (findPrice)` |
| 2 | 実行 | `catalog<br>    .filter((entry) => entry.provider === event.provider)<br>    .filter((entry) => entry.region === event.region \|\| entry.region === "*")<br>    ` | `filter` | `catalog<br>    .filter((entry) => entry.provider === event.provider)<br>    .filter((entry) => entry.region === event.region \|\| entry.region === "*")<br>    ` へ filter を実行する。 | `findPrice` | `apps/api/src/rag/_shared/usage/usage-pricing-catalog.ts:69 (findPrice)` |
| 3 | 実行 | `catalog<br>    .filter((entry) => entry.provider === event.provider)<br>    .filter((entry) => entry.region === event.region \|\| entry.region === "*")<br>    .filter((entry) => entry.modelId === event.modelId \|\| entry.modelId === "*")<br>    ` | `filter` | `catalog<br>    .filter((entry) => entry.provider === event.provider)<br>    .filter((entry) => entry.region === event.region \|\| entry.region === "*")<br>    .filter((entry) => entry.modelId === event.modelId \|\| entry.modelId === "*")<br>    ` へ filter を実行する。 | `findPrice` | `apps/api/src/rag/_shared/usage/usage-pricing-catalog.ts:69 (findPrice)` |

## 解析境界

- store interface call は API 実装が要求する論理操作です。local / DynamoDB / S3 など実 adapter の選択は `createDependencies()` の runtime 設定に従います。
- interface 経由で複数 adapter があり TypeScript symbol が一意にならない場合、実装を推測せず interface call を記載します。
- 文書生成専用 query metadata や API 別 CRUD 定義は参照していません。
