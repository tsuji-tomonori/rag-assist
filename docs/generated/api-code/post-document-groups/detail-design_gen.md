<!-- This file is generated by npm run docs:api-code. Do not edit manually. -->

# POST /document-groups 詳細設計

## 1. API の役割

文書グループを作成する

文書をスコープごとに整理するための文書グループを作成します。

| 項目 | 内容 |
| --- | --- |
| Method | `POST` |
| Path | `/document-groups` |
| OpenAPI contract | あり（runtime `/openapi.json`） |
| Route 定義 | `apps/api/src/routes/document-routes.ts:233 (POST /document-groups)` |
| Handler | `apps/api/src/routes/document-routes.ts:249 (POST /document-groups handler)` |

## 2. 主処理

| # | 深さ | 種別 | 自然言語での処理 | コード根拠 | 実装位置 |
| ---: | ---: | --- | --- | --- | --- |
| 1 | 0 | 認証・認可 | 認証済み利用者を request context から取得する。 | `user = c.get("user")` | `apps/api/src/routes/document-routes.ts:250` |
| 2 | 0 | 認証・認可 | "rag:group:create" permission を必須条件として確認する。 | `requirePermission(user, "rag:group:create")` | `apps/api/src/routes/document-routes.ts:251` |
| 3 | 0 | 入力検証 | schema 検証済みの JSON request body を取得する。 | `body = validJson<z.infer<typeof CreateDocumentGroupRequestSchema>>(c)` | `apps/api/src/routes/document-routes.ts:252` |
| 4 | 0 | 分岐 | 分岐: document group has legacy explicit policy の判定結果が真である。 | `documentGroupHasLegacyExplicitPolicy(body)` | `apps/api/src/routes/document-routes.ts:253` |
| 5 | 1 | 認証・認可 | "rag:group:assign_manager" permission を必須条件として確認する。 | `requirePermission(user, "rag:group:assign_manager")` | `apps/api/src/routes/document-routes.ts:253` |
| 6 | 0 | 状態 | 例外を監視しながら主要処理を実行する。 | `try` | `apps/api/src/routes/document-routes.ts:254` |
| 7 | 1 | 応答 | service の create document group 処理を呼び出す。 その結果を HTTP 200 の JSON response として返す。 | `return c.json(await service.createDocumentGroup(user, body), 200)` | `apps/api/src/routes/document-routes.ts:255` |
| 8 | 0 | 例外 | 例外を捕捉し、種類と内容に応じて HTTP 応答へ変換する。 | `catch (err) { if (isDocumentGroupInputError(err)) return c.json({ error: (err as Error).message }, 400) if (err instanceof Error && err.message.startsWith("Forbidden:")) throw new HTTPException(403, { message: "Forbidde…` | `apps/api/src/routes/document-routes.ts:256` |
| 9 | 1 | 分岐 | 分岐: is document group input error の判定結果が真である。 | `isDocumentGroupInputError(err)` | `apps/api/src/routes/document-routes.ts:257` |
| 10 | 2 | 応答 | 処理結果を HTTP 400 の JSON response として返す。 | `return c.json({ error: (err as Error).message }, 400)` | `apps/api/src/routes/document-routes.ts:257` |
| 11 | 1 | 分岐 | 分岐: `err` が `Error` の instance である、かつ starts with の判定結果が真である。 | `err instanceof Error && err.message.startsWith("Forbidden:")` | `apps/api/src/routes/document-routes.ts:258` |
| 12 | 2 | 例外 | HTTPException (403) を送出して通常処理を中断する。 | `throw new HTTPException(403, { message: "Forbidden" })` | `apps/api/src/routes/document-routes.ts:258` |
| 13 | 1 | 例外 | err を例外として送出する。 | `throw err` | `apps/api/src/routes/document-routes.ts:259` |

## 3. 条件分岐・例外

| # | 所属関数 | 種別 | 条件の意味 | 根拠式 | 実装位置 |
| ---: | --- | --- | --- | --- | --- |
| B001 | `POST /document-groups handler` | if | document group has legacy explicit policy の判定結果が真である | `documentGroupHasLegacyExplicitPolicy(body)` | `apps/api/src/routes/document-routes.ts:253 (POST /document-groups handler)` |
| B002 | `POST /document-groups handler` | catch | 例外が発生した場合に catch 処理へ移る | `err` | `apps/api/src/routes/document-routes.ts:256 (POST /document-groups handler)` |
| B003 | `POST /document-groups handler` | if | is document group input error の判定結果が真である | `isDocumentGroupInputError(err)` | `apps/api/src/routes/document-routes.ts:257 (POST /document-groups handler)` |
| B004 | `POST /document-groups handler` | if | `err` が `Error` の instance である、かつ starts with の判定結果が真である | `err instanceof Error && err.message.startsWith("Forbidden:")` | `apps/api/src/routes/document-routes.ts:258 (POST /document-groups handler)` |
| B005 | `requirePermission` | if | 利用者が 指定された permission を持たない | `!hasPermission(user, permission)` | `apps/api/src/authorization.ts:267 (requirePermission)` |
| B006 | `MemoRagService.createDocumentGroup` | 三項条件 | `input.parentGroupId` が存在し、真である | `input.parentGroupId` | `apps/api/src/rag/memorag-service.ts:448 (MemoRagService.createDocumentGroup)` |
| B007 | `MemoRagService.createDocumentGroup` | if | `input.parentGroupId` が存在し、真である、かつ `parent` が存在しない、または偽である | `input.parentGroupId && !parent` | `apps/api/src/rag/memorag-service.ts:449 (MemoRagService.createDocumentGroup)` |
| B008 | `MemoRagService.createDocumentGroup` | if | `parent` が存在し、真である、かつ can manage document group の判定結果が真ではない | `parent && !canManageDocumentGroup(parent, actor, groups)` | `apps/api/src/rag/memorag-service.ts:450 (MemoRagService.createDocumentGroup)` |
| B009 | `MemoRagService.createDocumentGroup` | if | some の判定結果が真である | `groups.some((group) => group.adminPathPk === pathFields.adminPathPk && group.normalizedCanonicalPath === pathFields.normalizedCanonicalPath)` | `apps/api/src/rag/memorag-service.ts:459 (MemoRagService.createDocumentGroup)` |
| B010 | `MemoRagService.createDocumentGroup` | 三項条件 | `parent` が存在し、真である | `parent` | `apps/api/src/rag/memorag-service.ts:474 (MemoRagService.createDocumentGroup)` |
| B011 | `MemoRagService.createDocumentGroup` | 三項条件 | `hasExplicitPolicy` が存在し、真である | `hasExplicitPolicy` | `apps/api/src/rag/memorag-service.ts:480 (MemoRagService.createDocumentGroup)` |

## 4. 到達する主要実装

handler を起点に TypeScript symbol を解決し、深さ 2 までの主要関数・method を列挙しています。深い helper を含む全到達関数は 27 件で、永続化・外部接続は深さにかかわらず次節へ集約しています。

| 深さ | Symbol | 責務 | 実装位置 |
| ---: | --- | --- | --- |
| 0 | `POST /document-groups handler` | POST /document-groups の request を受け、検証・認可・service 呼び出し・HTTP 応答を調整する。 | `apps/api/src/routes/document-routes.ts:249 (POST /document-groups handler)` |
| 1 | `requirePermission` | require permission の実装処理を担当する。 | `apps/api/src/authorization.ts:266 (requirePermission)` |
| 2 | `hasPermission` | has permission の実装処理を担当する。 | `apps/api/src/authorization.ts:270 (hasPermission)` |
| 1 | `validJson` | valid json の実装処理を担当する。 | `apps/api/src/routes/route-utils.ts:20 (validJson)` |
| 2 | `validRequest` | valid request の実装処理を担当する。 | `apps/api/src/routes/route-utils.ts:32 (validRequest)` |
| 1 | `documentGroupHasLegacyExplicitPolicy` | document group has legacy explicit policy の実装処理を担当する。 | `apps/api/src/folders/document-group-permissions.ts:11 (documentGroupHasLegacyExplicitPolicy)` |
| 1 | `MemoRagService.createDocumentGroup` | create document group の実装処理を担当する。 | `apps/api/src/rag/memorag-service.ts:434 (MemoRagService.createDocumentGroup)` |
| 2 | `validateDocumentGroupName` | validate document group name の実装処理を担当する。 | `apps/api/src/rag/memorag-service.ts:3046 (validateDocumentGroupName)` |
| 2 | `normalizeDocumentGroups` | normalize document groups の実装処理を担当する。 | `apps/api/src/rag/memorag-service.ts:3172 (normalizeDocumentGroups)` |
| 2 | `canManageDocumentGroup` | can manage document group の実装処理を担当する。 | `apps/api/src/folders/document-group-permissions.ts:27 (canManageDocumentGroup)` |
| 2 | `forbiddenError` | forbidden error の実装処理を担当する。 | `apps/api/src/rag/memorag-service.ts:3268 (forbiddenError)` |
| 2 | `resolveDocumentGroupAdminPrincipal` | resolve document group admin principal の実装処理を担当する。 | `apps/api/src/rag/memorag-service.ts:3065 (resolveDocumentGroupAdminPrincipal)` |
| 1 | `isDocumentGroupInputError` | is document group input error の実装処理を担当する。 | `apps/api/src/routes/document-routes.ts:882 (isDocumentGroupInputError)` |

## 5. データ・外部境界

| 種別 | 境界 | Target | Operation | 目的 | Caller | 実装位置 |
| --- | --- | --- | --- | --- | --- | --- |
| 参照 | Store | `this.deps.documentGroupStore` | `list` | `this.deps.documentGroupStore` に対して list を実行する。 | `MemoRagService.createDocumentGroup` | `apps/api/src/rag/memorag-service.ts:447 (MemoRagService.createDocumentGroup)` |
| 作成・追記 | Store | `this.deps.documentGroupStore` | `createWithPathLock` | `this.deps.documentGroupStore` に対して create with path lock を実行する。 | `MemoRagService.createDocumentGroup` | `apps/api/src/rag/memorag-service.ts:486 (MemoRagService.createDocumentGroup)` |

## 6. 応答・メッセージ

| 種別 | Status/Event | 内容 | 発生条件 |
| --- | --- | --- | --- |
| OpenAPI contract | `200` | リクエストは成功し、レスポンス body に結果を返します。 | OpenAPI で宣言された HTTP 200 response |
| OpenAPI contract | `401` | 認証が必要です。 | OpenAPI で宣言された HTTP 401 response |
| OpenAPI contract | `403` | 対象操作を実行する権限がありません。 | OpenAPI で宣言された HTTP 403 response |
| 例外 | `403` | Forbidden | 利用者が 指定された permission を持たない |
| 例外 | `-` | Document group name is required | `trimmed` が存在しない、または偽である |
| 例外 | `-` | Document group name contains unsupported characters | `trimmed` が "/" を含む、または contains control character の判定結果が真である |
| 例外 | `-` | Parent document group not found | `input.parentGroupId` が存在し、真である、かつ `parent` が存在しない、または偽である |
| 例外 | `-` | adminPrincipalId is required | `adminPrincipalId` が存在しない、または偽である |
| 例外 | `-` | Document group canonical path already exists | some の判定結果が真である |
| 例外 | `403` | Forbidden | `err` が `Error` の instance である、かつ starts with の判定結果が真である |

## 7. テスト対応

| 関連 | Test case | 実装位置 |
| --- | --- | --- |
| 到達 symbol | service manages async agent run metadata without provider execution or mock artifacts | `apps/api/src/rag/memorag-service.test.ts:94 (service manages async agent run metadata without provider execution or mock artifacts)` |
| 到達 symbol | service listDocuments denies group-scoped manifests to non-members without legacy ACLs | `apps/api/src/rag/memorag-service.test.ts:412 (service listDocuments denies group-scoped manifests to non-members without legacy ACLs)` |
| 到達 symbol | service stores document group hierarchy outside S3 object keys | `apps/api/src/rag/memorag-service.test.ts:443 (service stores document group hierarchy outside S3 object keys)` |
| 到達 symbol | service assigns canonical document group paths and rejects duplicate paths per admin principal | `apps/api/src/rag/memorag-service.test.ts:482 (service assigns canonical document group paths and rejects duplicate paths per admin principal)` |
| 到達 symbol | service recalculates descendant canonical paths and local lock items on move and rename | `apps/api/src/rag/memorag-service.test.ts:517 (service recalculates descendant canonical paths and local lock items on move and rename)` |
| 到達 symbol | service moves a document group subtree back to root with path locks | `apps/api/src/rag/memorag-service.test.ts:543 (service moves a document group subtree back to root with path locks)` |
| 到達 symbol | service enforces document group management and search scope boundaries | `apps/api/src/rag/memorag-service.test.ts:606 (service enforces document group management and search scope boundaries)` |
| 到達 symbol | service inherits parent document group sharing unless child has explicit policy | `apps/api/src/rag/memorag-service.test.ts:673 (service inherits parent document group sharing unless child has explicit policy)` |
| 到達 symbol | service annotates visible document groups with effective permission and inheritance source | `apps/api/src/rag/memorag-service.test.ts:836 (service annotates visible document groups with effective permission and inheritance source)` |
| 到達 symbol | service does not expose group scoped documents to ownerUserId without folder read permission | `apps/api/src/rag/memorag-service.test.ts:867 (service does not expose group scoped documents to ownerUserId without folder read permission)` |
| 到達 symbol | service does not allow group scoped document owner to delete or reindex without folder full permission | `apps/api/src/rag/memorag-service.test.ts:891 (service does not allow group scoped document owner to delete or reindex without folder full permission)` |
| 到達 symbol | search excludes owner-owned group scoped documents when folder permission is none | `apps/api/src/rag/memorag-service.test.ts:921 (search excludes owner-owned group scoped documents when folder permission is none)` |
| 到達 symbol | service enforces full document group permission for delete and reindex operations | `apps/api/src/rag/memorag-service.test.ts:952 (service enforces full document group permission for delete and reindex operations)` |
| 到達 symbol | service search denies group-scoped manifests to non-members without legacy ACLs | `apps/api/src/search/hybrid-search.test.ts:342 (service search denies group-scoped manifests to non-members without legacy ACLs)` |

## 8. 解析上の注意

- 既知の operation 固有 warning はありません。

- 記載内容は route、runtime OpenAPI、handler から到達する repository 内 symbol、既存 test を解析した派生成果物です。
- 可読性のため、詳細設計と unit test の分岐は handler と直下関数（深さ 1 まで）へ絞り、深い共通 helper の内部分岐は API 固有要件として重複掲載しません。
- 文書生成専用 annotation、decorator、コメント、API 別設定表は入力にしていません。
- interface / dynamic dispatch の実装先を TypeScript checker が一意に解決できない場合、logical store call までを確定情報として記載します。
