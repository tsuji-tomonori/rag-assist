<!-- This file is generated by npm run docs:api-code. Do not edit manually. -->

# POST /benchmark/query メッセージ仕様

## 生成根拠

- OpenAPI response description
- handler と到達関数の `c.json({ error/message })`
- `HTTPException` / `Error` の literal message
- logger call と SSE event の literal message

## メッセージ一覧

| ID | 種別 | Status/Event | メッセージ | 発生条件 | 実装根拠 |
| --- | --- | --- | --- | --- | --- |
| M001 | OpenAPI contract | `200` | リクエストは成功し、レスポンス body に結果を返します。 | OpenAPI で宣言された HTTP 200 response | runtime OpenAPI |
| M002 | OpenAPI contract | `400` | リクエスト形式または入力値が不正です。 | OpenAPI で宣言された HTTP 400 response | runtime OpenAPI |
| M003 | OpenAPI contract | `401` | 認証が必要です。 | OpenAPI で宣言された HTTP 401 response | runtime OpenAPI |
| M004 | OpenAPI contract | `403` | 対象操作を実行する権限がありません。 | OpenAPI で宣言された HTTP 403 response | runtime OpenAPI |
| M005 | OpenAPI contract | `503` | benchmark 評価が無効または設定不備のため利用できません | OpenAPI で宣言された HTTP 503 response | runtime OpenAPI |
| M006 | 例外 | `403` | Forbidden | 利用者が 指定された permission を持たない | `apps/api/src/authorization.ts:184 (requirePermission)` |
| M007 | 例外 | `-` | Forbidden | is dedicated benchmark runner の判定結果が真ではない | `apps/api/src/benchmark/evaluation-context.ts:77 (resolveBenchmarkEvaluationContext)` |
| M008 | 例外 | `-` | Benchmark evaluation is unavailable | `settings.enabled` が存在しない、または偽である | `apps/api/src/benchmark/evaluation-context.ts:80 (resolveBenchmarkEvaluationContext)` |
| M009 | 例外 | `-` | Benchmark evaluation is unavailable | `tenantId` が存在しない、または偽である、または `tenantId` が `runner.tenantId` と等しい | `apps/api/src/benchmark/evaluation-context.ts:85 (resolveBenchmarkEvaluationContext)` |
| M010 | 例外 | `-` | Unknown benchmark suite | `registration` が存在しない、または偽である、または `suiteId` が `requestedSuiteId` と異なる | `apps/api/src/benchmark/evaluation-context.ts:91 (resolveBenchmarkEvaluationContext)` |
| M011 | 例外 | `-` | worker_tenant_missing | `tenantId` が存在しない、または偽である | `apps/api/src/rag/memorag-service.ts:4655 (MemoRagService.assertCurrentWorkerAuthorization)` |
| M012 | 例外 | `-` | worker_identity_not_canonical | canonical の判定結果が真ではない、または canonical の判定結果が真ではない、または canonical の判定結果が真ではない | `apps/api/src/security/current-worker-authorization.ts:46 (CurrentWorkerAuthorization.assertAuthorized)` |
| M013 | 例外 | `-` | authoritative_identity_unavailable | 例外を捕捉した場合 | `apps/api/src/security/current-worker-authorization.ts:53 (CurrentWorkerAuthorization.assertAuthorized)` |
| M014 | 例外 | `-` | account_deleted | `identity` が存在しない、または偽である | `apps/api/src/security/current-worker-authorization.ts:55 (CurrentWorkerAuthorization.assertAuthorized)` |
| M015 | 例外 | `-` | subject_mismatch | `identity.userId` が `request.subject` と異なる | `apps/api/src/security/current-worker-authorization.ts:56 (CurrentWorkerAuthorization.assertAuthorized)` |
| M016 | 例外 | `-` | account_inactive | `identity.accountStatus` が `"active"` と異なる | `apps/api/src/security/current-worker-authorization.ts:57 (CurrentWorkerAuthorization.assertAuthorized)` |
| M017 | 例外 | `-` | tenant_membership_revoked | `identity.tenantId` が `request.tenantId` と異なる | `apps/api/src/security/current-worker-authorization.ts:58 (CurrentWorkerAuthorization.assertAuthorized)` |
| M018 | 例外 | `-` | role_permission_revoked | every の判定結果が真ではない | `apps/api/src/security/current-worker-authorization.ts:69 (CurrentWorkerAuthorization.assertAuthorized)` |
| M019 | 例外 | `-` | resource_policy_revoked | `resourceAllowed` が存在しない、または偽である | `apps/api/src/security/current-worker-authorization.ts:78 (CurrentWorkerAuthorization.assertAuthorized)` |
| M020 | 例外 | `-` | authoritative_identity_provider_missing | `config.authEnabled` が存在し、真である | `apps/api/src/rag/memorag-service.ts:4664 (MemoRagService.assertCurrentWorkerAuthorization)` |
| M021 | 例外 | `-` | local_fixture_permission_missing | `config.nodeEnv` が `"test"` と異なる、かつ every の判定結果が真ではない | `apps/api/src/rag/memorag-service.ts:4674 (MemoRagService.assertCurrentWorkerAuthorization)` |
| M022 | 例外 | `-` | local_fixture_resource_policy_denied | `resourceAllowed` が存在しない、または偽である | `apps/api/src/rag/memorag-service.ts:4682 (MemoRagService.assertCurrentWorkerAuthorization)` |
| M023 | 例外 | `-` | `Security resource ${name} is invalid` | `value` が存在しない、または偽である、または `value.trim()` が `value` と異なる | `apps/api/src/security/security-resource-reference.ts:12 (securityResourceReference)` |
| M024 | 例外 | `-` | Run admission resource-group identity crossed its tenant boundary | `membership.tenantId` が `tenantId` と異なる、または `membership.memberId` が `groupId` と異なる | `apps/api/src/rag/memorag-service.ts:1376 (MemoRagService.securityResourceRefsForActor)` |
| M025 | 例外 | `-` | RAG safety state is unavailable. | `required` が存在し、真である | `apps/api/src/rag/quality-control/production-rag-monitor.ts:313 (assertRagSafetyInterlock)` |
| M026 | 例外 | `-` | RAG safety state is invalid or unsupported. | `state.schemaVersion` が `1` と異なる、または is integer の判定結果が真ではない、または `state.stateVersion` が `1` より小さい、または trim の判定結果が真ではない、または trim の判定結果が真ではない、または trim の判定結果が真ではない、または is array の判定結果が真ではない、または some の判定結果が真である、または `typeof state.promotionFrozen` が `"boolean"` と異なる、または `typeof state.documentQuarantineRequired` が `"boolean"` と異なる、または `["normal", "limited", "refuse"]` が state.responseMode を含まない、または is finite の判定結果が真ではない、または is finite の判定結果が真ではない、または `Date.parse(state.validUntil)` が `Date.parse(state.updatedAt)` 以下である | `apps/api/src/rag/quality-control/production-rag-monitor.ts:362 (assertRagSafetyState)` |
| M027 | 例外 | `-` | RAG safety state is expired. | is finite の判定結果が真ではない、または `Date.parse(state.validUntil)` が `Date.parse(now)` 以下である | `apps/api/src/rag/quality-control/production-rag-monitor.ts:319 (assertRagSafetyInterlock)` |
| M028 | 例外 | `-` | The requested RAG runtime is not the active monitored runtime. | `state.activeRuntimeProfileVersion` が `input.runtimeProfileVersion` と異なる | `apps/api/src/rag/quality-control/production-rag-monitor.ts:322 (assertRagSafetyInterlock)` |
| M029 | 例外 | `-` | `RAG ${state.responseMode} response mode is active.` | `operation` が `"chat"` と等しい、または `operation` が `"search"` と等しい、かつ `state.responseMode` が `"normal"` と異なる | `apps/api/src/rag/quality-control/production-rag-monitor.ts:329 (assertRagSafetyInterlock)` |
| M030 | 例外 | `-` | RAG promotion is frozen by the monitoring control loop. | `operation` が `"publication"` と等しい、または `operation` が `"promotion"` と等しい、かつ `state.promotionFrozen` が存在し、真である | `apps/api/src/rag/quality-control/production-rag-monitor.ts:332 (assertRagSafetyInterlock)` |
| M031 | 例外 | `-` | RAG document publication is blocked while quarantine is required. | `operation` が `"publication"` と等しい、かつ `state.documentQuarantineRequired` が存在し、真である | `apps/api/src/rag/quality-control/production-rag-monitor.ts:335 (assertRagSafetyInterlock)` |
| M032 | 例外 | `-` | Forbidden | `requestedTenantId` が存在し、真である、かつ `requestedTenantId` が `trustedTenantId` と異なる | `apps/api/src/rag/online/retrieval/hybrid/hybrid-retriever.ts:1359 (resolveAuthoritativeSearchTenant)` |
| M033 | 例外 | `-` | Forbidden | `config.authEnabled` が存在し、真である | `apps/api/src/rag/online/retrieval/hybrid/hybrid-retriever.ts:1362 (resolveAuthoritativeSearchTenant)` |
| M034 | 例外 | `-` | Local/test tenant is not configured | `explicitLocalTenant` が存在しない、または偽である | `apps/api/src/rag/online/retrieval/hybrid/hybrid-retriever.ts:1364 (resolveAuthoritativeSearchTenant)` |
| M035 | 例外 | `-` | Artifact key is invalid | `normalized` が存在しない、または偽である、または `normalized` が ".." を含む | `apps/api/src/rag/_shared/storage/tenant-artifacts.ts:127 (normalizeRelativeKey)` |
| M036 | 例外 | `-` | Authoritative tenant is required | `normalized` が存在しない、または偽である | `apps/api/src/rag/_shared/storage/tenant-artifacts.ts:121 (requiredTenantId)` |
| M037 | 例外 | `-` | Authoritative tenant is required | `normalized` が存在しない、または偽である | `apps/api/src/security/tenant-partition.ts:5 (tenantPartitionId)` |
| M038 | 例外 | `-` | `Document manifest tenant mismatch${key ? `: ${key}` : ""}` | `manifestTenantId` が存在しない、または偽である、かつ `options.allowMissingTenant` が存在しない、または偽である、または `manifestTenantId` が存在し、真である、かつ `manifestTenantId` が `normalizedTenantId` と異なる | `apps/api/src/rag/_shared/storage/tenant-artifacts.ts:111 (assertManifestTenant)` |
| M039 | 例外 | `-` | Document manifest escaped its authoritative tenant partition | uses legacy global document artifacts の判定結果が真ではない、かつ starts with の判定結果が真ではない | `apps/api/src/rag/_shared/storage/tenant-artifacts.ts:96 (readTenantManifestByKey)` |
| M040 | 例外 | `-` | `${field} is invalid` | `normalized` が存在しない、または偽である、または `normalized.length` が `512` より大きい、または `hasControlCharacter` が存在し、真である | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:651 (canonicalIdentifier)` |
| M041 | 例外 | `-` | `${field} is invalid` | is finite の判定結果が真ではない | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:662 (canonicalTimestamp)` |
| M042 | 例外 | `-` | Unsupported revocation trigger | `REVOCATION_TRIGGERS` が input.trigger を含まない | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:475 (normalizeRegistration)` |
| M043 | 例外 | `-` | Unsupported cleanup scope | `REVOCATION_CLEANUP_SCOPES` が target.scope を含まない | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:561 (normalizeTargetReferences)` |
| M044 | 例外 | `-` | Cleanup discovery returned the wrong scope | `requiredScope` が存在し、真である、かつ `target.scope` が `requiredScope` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:562 (normalizeTargetReferences)` |
| M045 | 例外 | `-` | Revocation cleanup tenant identity is invalid | `value` が存在しない、または偽である、または `value.trim()` が `value` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-tenant-registry.ts:140 (canonicalTenantId)` |
| M046 | 例外 | `-` | Revocation cleanup tenant registry record is invalid | `record.schemaVersion` が `schemaVersion` と異なる、または `record.tenantId` が `tenantId` と異なる、または `record.tenantPartitionId` が `tenantPartitionId(tenantId)` と異なる、または `key` が `tenantRegistryKey(tenantId)` と異なる、または is finite の判定結果が真ではない、または is finite の判定結果が真ではない | `apps/api/src/rag/_shared/security/revocation-cleanup-tenant-registry.ts:136 (assertRecord)` |
| M047 | 例外 | `-` | Revocation cleanup manifest is invalid | `manifest.schemaVersion` が `1` と異なる、または `manifest.policyVersion` が `REVOCATION_CLEANUP_POLICY_VERSION` と異なる、または `manifest.tenantId` が `tenantId` と異なる、または `manifest.operationId` が `operationId` と異なる、または `canonicalIdentifier(manifest.tenantId, "tenantId")` が `manifest.tenantId` と異なる、または `canonicalIdentifier(manifest.operationId, "operationId")` が `manifest.operationId` と異なる、または `canonicalIdentifier(manifest.resourceId, "resourceId")` が `manifest.resourceId` と異なる、または `["document", "temporary_attachment", "folder", "resource_group", "benchmark_run", "account", "group"]` が manifest.resourceType を含まない、または `REVOCATION_TRIGGERS` が manifest.trigger を含まない、または `manifest.authoritativeDeny?.status` が `"effective"` と異なる、または `canonicalIdentifier(manifest.authoritativeDeny.version, "authoritativeDenyVersion")` が `manifest.authoritativeDeny.version` と異なる、または `canonicalTimestamp(manifest.authoritativeDeny.confirmedAt, "authoritativeDenyConfirmedAt")` が `manifest.authoritativeDeny.confirmedAt` と異なる、または `["cleanup_pending", "reconciliation_required", "completed", "superseded"]` が manifest.status を含まない、または is array の判定結果が真ではない、または some の判定結果が真である、または is safe integer の判定結果が真ではない、または `manifest.attempts` が `0` より小さい、または `canonicalTimestamp(manifest.createdAt, "createdAt")` が `manifest.createdAt` と異なる、または `canonicalTimestamp(manifest.updatedAt, "updatedAt")` が `manifest.updatedAt` と異なる、または is array の判定結果が真ではない、または is array の判定結果が真ではない、または `manifest.scopes.length` が `REVOCATION_CLEANUP_SCOPES.length` と異なる、または every の判定結果が真ではない | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:506 (validateManifest)` |
| M048 | 例外 | `-` | Revocation cleanup scope state is invalid | `REVOCATION_CLEANUP_SCOPES` が scope.scope を含まない、または `["pending", "verified"]` が scope.status を含まない | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:509 (validateManifest)` |
| M049 | 例外 | `-` | Revocation cleanup scope timestamp is invalid | `timestamp` が `undefined` と異なる、かつ `canonicalTimestamp(timestamp, "scopeTimestamp")` が `timestamp` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:513 (validateManifest)` |
| M050 | 例外 | `-` | Revocation cleanup target state is invalid | `REVOCATION_CLEANUP_SCOPES` が target.scope を含まない、または `canonicalIdentifier(target.reference, "cleanupTargetReference")` が `target.reference` と異なる、または `target.targetId` が `cleanupTargetId(target)` と異なる、または `["pending", "cleaned"]` が target.status を含まない、または is safe integer の判定結果が真ではない、または `target.attempts` が `0` より小さい、または has の判定結果が真である | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:527 (validateManifest)` |
| M051 | 例外 | `-` | Revocation cleanup target timestamp is invalid | `timestamp` が `undefined` と異なる、かつ `canonicalTimestamp(timestamp, "targetTimestamp")` が `timestamp` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:531 (validateManifest)` |
| M052 | 例外 | `-` | Revocation cleanup idempotency key was reused with different input | `manifest.resourceType` が `input.resourceType` と異なる、または `manifest.resourceId` が `input.resourceId` と異なる、または `manifest.trigger` が `input.trigger` と異なる、または `manifest.authoritativeDeny.version` が `input.authoritativeDenyVersion` と異なる、または `manifest.authoritativeDeny.confirmedAt` が `input.authoritativeDenyConfirmedAt` と異なる、または `JSON.stringify(manifest.deniedPurposes)` が `JSON.stringify(input.deniedPurposes)` と異なる | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:549 (assertSameRegistration)` |
| M053 | 例外 | `-` | Revocation cleanup idempotency key was reused with different targets | some の判定結果が真である | `apps/api/src/rag/_shared/security/revocation-cleanup-coordinator.ts:552 (assertSameRegistration)` |
| M054 | 例外 | `-` | Source identity, tenant, owner, or version is missing | `sourceId` が存在しない、または偽である、または `sourceVersion` が存在しない、または偽である、または `tenantId` が存在しない、または偽である、または `ownerUserId` が存在しない、または偽である | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:872 (sourceIdentity)` |
| M055 | 例外 | `-` | Source governance identity changed | `record.schemaVersion` が `1` と異なる、または `record.sourceId` が `identity.sourceId` と異なる、または `record.tenantId` が `identity.tenantId` と異なる、または `record.ownerUserId` が `identity.ownerUserId` と異なる、または `isPublishedArtifact` が存在しない、または偽である、かつ `record.sourceVersion` が `identity.sourceVersion` と異なる | `apps/api/src/rag/offline/pre-retrieval/admission/source-governance-approval-service.ts:893 (assertRecordIdentity)` |
| M056 | 例外 | `-` | Invalid RAG observation timestamp | is finite の判定結果が真ではない | `apps/api/src/rag/quality-control/production-rag-observation-producer.ts:1113 (normalizeTimestamp)` |
| M057 | 例外 | `-` | Invalid RAG quality source sample identity | `sample.schemaVersion` が `1` と異なる、または `sample.signalCatalogVersion` が `RAG_QUALITY_SIGNAL_CATALOG_VERSION` と異なる、または trim の判定結果が真ではない、または trim の判定結果が真ではない、または trim の判定結果が真ではない、または trim の判定結果が真ではない、または trim の判定結果が真ではない、または `sample.sourceType` が存在しない、または偽である、または trim の判定結果が真ではない、または `sample.tenantPartitionId` が `undefined` と異なる、かつ test の判定結果が真ではない、または `sample.resourceIds` が `undefined` と異なる、かつ is array の判定結果が真ではない、または some の判定結果が真である、または `sample.securityResourceRefs` が `undefined` と異なる、かつ is array の判定結果が真ではない、または some の判定結果が真である、または trim の判定結果が真ではない、または is finite の判定結果が真ではない | `apps/api/src/rag/quality-control/production-rag-observation-producer.ts:1054 (assertSourceSample)` |
| M058 | 例外 | `-` | `Unknown RAG quality signal: ${signalId}` | `RAG_REQUIRED_SIGNAL_IDS` が signalId as RagQualitySignalId を含まない | `apps/api/src/rag/quality-control/production-rag-observation-producer.ts:1056 (assertSourceSample)` |
| M059 | 例外 | `-` | `Available RAG quality measurement is incomplete: ${signalId}` | is integer の判定結果が真ではない、または `item.sampleCount` が `0` より小さい、または `item.available` が存在し、真である、かつ `item.value` が `null` と等しい、または is finite の判定結果が真ではない、または `item.sampleCount` が `1` より小さい、または `item.confidence` が `null` と等しい、または `item.confidence` が `0` より小さい、または `item.confidence` が `1` より大きい | `apps/api/src/rag/quality-control/production-rag-observation-producer.ts:1069 (assertSourceSample)` |
| M060 | 例外 | `-` | `Unavailable RAG quality measurement must not contain a value: ${signalId}` | `item.available` が存在しない、または偽である、かつ `item.value` が `null` と異なる、または `item.confidence` が `null` と異なる | `apps/api/src/rag/quality-control/production-rag-observation-producer.ts:1072 (assertSourceSample)` |
| M061 | ログ | `-` | RAG quality source sample was not recorded | `result.skippedReason` が存在し、真である、かつ `process.env.RAG_MONITORING_REQUIRED` が `"1"` と等しい | `apps/api/src/rag/quality-control/production-rag-observation-producer.ts:828 (bestEffortCapture)` |
| M062 | ログ | `-` | RAG quality source sample persistence failed | 例外を捕捉した場合 | `apps/api/src/rag/quality-control/production-rag-observation-producer.ts:831 (bestEffortCapture)` |
| M063 | 例外 | `-` | Lexical cache fallback blocked because mandatory guard outcomes were not observed | `artifactLoad.degradationDecision?.action` が `"fail"` と等しい | `apps/api/src/rag/online/retrieval/hybrid/hybrid-retriever.ts:663 (getLexicalIndex)` |
| M064 | 例外 | `-` | `Stored chunk policy cannot be reproduced: ${result.violations.map((violation) => violation.code).join(",")}` | `result.publicationEligible` が存在しない、または偽である | `apps/api/src/rag/_shared/storage/manifest-chunks.ts:18 (loadChunksForManifest)` |
| M065 | 例外 | `-` | Stored chunk manifest does not reconcile with its versioned policy | `expectedIds.length` が `0` より大きい、かつ same strings の判定結果が真ではない | `apps/api/src/rag/_shared/storage/manifest-chunks.ts:21 (loadChunksForManifest)` |
| M066 | 例外 | `-` | Document identifier is required | `normalized` が存在しない、または偽である | `apps/api/src/rag/_shared/storage/tenant-artifacts.ts:133 (encodeIdentifier)` |
| M067 | 例外 | `-` | Forbidden | `requestedTenantId` が存在し、真である、かつ `requestedTenantId` が `tenantId` と異なる | `apps/api/src/chat-orchestration/nodes/retrieve-memory.ts:176 (resolveCurrentMemoryTenant)` |
| M068 | 例外 | `-` | Forbidden | `config.authEnabled` が存在し、真である | `apps/api/src/chat-orchestration/nodes/retrieve-memory.ts:179 (resolveCurrentMemoryTenant)` |
| M069 | 例外 | `-` | Local/test tenant is not configured | `localTenantId` が存在しない、または偽である | `apps/api/src/chat-orchestration/nodes/retrieve-memory.ts:181 (resolveCurrentMemoryTenant)` |
| M070 | 例外 | `-` | Debug trace tenant partition is required | `trace.tenantPartitionId` が存在しない、または偽である | `apps/api/src/rag/orchestration/chat-rag-orchestrator.ts:1225 (debugTraceObjectKey)` |
| M071 | 例外 | `-` | Quality sample deletion tenant is invalid | trim の判定結果が真ではない、または `tenantId.trim()` が `tenantId` と異なる | `apps/api/src/rag/quality-control/production-rag-observation-producer.ts:297 (ProductionRagObservationProducer.deleteArtifactSamples)` |

## 読み方

- `contract` は API contract 上の応答説明であり、実装 literal と同一とは限りません。
- `http-response` / `exception` / `event` / `log` は実コードに存在する literal を根拠にしています。
- 動的に組み立てる文字列は式として確定できる場合だけ掲載し、推測した文面は補いません。
