<!-- This file is generated by npm run docs:api-code. Do not edit manually. -->

# GET /admin/usage 詳細設計

## 1. API の役割

利用状況を取得する

全ユーザーまたは指定条件に一致する利用状況の集計を返します。

| 項目 | 内容 |
| --- | --- |
| Method | `GET` |
| Path | `/admin/usage` |
| OpenAPI contract | あり（runtime `/openapi.json`） |
| Route 定義 | `apps/api/src/routes/admin-routes.ts:619 (GET /admin/usage)` |
| Handler | `apps/api/src/routes/admin-routes.ts:629 (GET /admin/usage handler)` |

## 2. 主処理

| # | 深さ | 種別 | 自然言語での処理 | コード根拠 | 実装位置 |
| ---: | ---: | --- | --- | --- | --- |
| 1 | 0 | 認証・認可 | 認証済み利用者を request context から取得する。 | `user = c.get("user")` | `apps/api/src/routes/admin-routes.ts:630` |
| 2 | 0 | 認証・認可 | "usage:read:all_users" permission を必須条件として確認する。 | `requirePermission(user, "usage:read:all_users")` | `apps/api/src/routes/admin-routes.ts:631` |
| 3 | 0 | 状態 | 例外を監視しながら主要処理を実行する。 | `try` | `apps/api/src/routes/admin-routes.ts:632` |
| 4 | 1 | 応答 | service の list usage summaries 処理を呼び出す。 その結果を HTTP 200 の JSON response として返す。 | `return c.json(await service.listUsageSummaries(user, validQuery<z.infer<typeof UsageQuerySchema>>(c)), 200)` | `apps/api/src/routes/admin-routes.ts:633` |
| 5 | 0 | 例外 | 例外を捕捉し、種類と内容に応じて HTTP 応答へ変換する。 | `catch (error) { if (isInvalidUsageQuery(error)) return c.json({ error: "Invalid usage query or cursor" }, 400) throw error }` | `apps/api/src/routes/admin-routes.ts:634` |
| 6 | 1 | 分岐 | 分岐: is invalid usage query の判定結果が真である。 | `isInvalidUsageQuery(error)` | `apps/api/src/routes/admin-routes.ts:635` |
| 7 | 2 | 応答 | 処理結果を HTTP 400 の JSON response として返す。 | `return c.json({ error: "Invalid usage query or cursor" }, 400)` | `apps/api/src/routes/admin-routes.ts:635` |
| 8 | 1 | 例外 | error を例外として送出する。 | `throw error` | `apps/api/src/routes/admin-routes.ts:636` |

## 3. 条件分岐・例外

| # | 所属関数 | 種別 | 条件の意味 | 根拠式 | 実装位置 |
| ---: | --- | --- | --- | --- | --- |
| B001 | `GET /admin/usage handler` | catch | 例外が発生した場合に catch 処理へ移る | `error` | `apps/api/src/routes/admin-routes.ts:634 (GET /admin/usage handler)` |
| B002 | `GET /admin/usage handler` | if | is invalid usage query の判定結果が真である | `isInvalidUsageQuery(error)` | `apps/api/src/routes/admin-routes.ts:635 (GET /admin/usage handler)` |
| B003 | `requirePermission` | if | 利用者が 指定された permission を持たない | `!hasPermission(user, permission)` | `apps/api/src/authorization.ts:184 (requirePermission)` |
| B004 | `MemoRagService.listUsageSummaries` | 三項条件 | `rolloutMode` が `"active"` と等しい、かつ `this.deps.usageEventStore` が存在し、真である | `rolloutMode === "active" && this.deps.usageEventStore` | `apps/api/src/rag/memorag-service.ts:2180 (MemoRagService.listUsageSummaries)` |

## 4. 到達する主要実装

handler を起点に TypeScript symbol を解決し、深さ 2 までの主要関数・method を列挙しています。深い helper を含む全到達関数は 18 件で、永続化・外部接続は深さにかかわらず次節へ集約しています。

| 深さ | Symbol | 責務 | 実装位置 |
| ---: | --- | --- | --- |
| 0 | `GET /admin/usage handler` | GET /admin/usage の request を受け、検証・認可・service 呼び出し・HTTP 応答を調整する。 | `apps/api/src/routes/admin-routes.ts:629 (GET /admin/usage handler)` |
| 1 | `requirePermission` | require permission の実装処理を担当する。 | `apps/api/src/authorization.ts:183 (requirePermission)` |
| 2 | `hasPermission` | has permission の実装処理を担当する。 | `apps/api/src/authorization.ts:187 (hasPermission)` |
| 1 | `validQuery` | valid query の実装処理を担当する。 | `apps/api/src/routes/route-utils.ts:28 (validQuery)` |
| 2 | `validRequest` | valid request の実装処理を担当する。 | `apps/api/src/routes/route-utils.ts:36 (validRequest)` |
| 1 | `MemoRagService.listUsageSummaries` | list usage summaries の実装処理を担当する。 | `apps/api/src/rag/memorag-service.ts:2176 (MemoRagService.listUsageSummaries)` |
| 2 | `authoritativeActorTenantId` | authoritative actor tenant id の実装処理を担当する。 | `apps/api/src/rag/memorag-service.ts:5877 (authoritativeActorTenantId)` |
| 2 | `defaultedUsageQuery` | defaulted usage query の実装処理を担当する。 | `apps/api/src/rag/memorag-service.ts:267 (defaultedUsageQuery)` |
| 2 | `MemoRagService.usageRolloutMode` | usage rollout mode の実装処理を担当する。 | `apps/api/src/rag/memorag-service.ts:5147 (MemoRagService.usageRolloutMode)` |
| 2 | `usageSummaryFromPage` | usage summary from page の実装処理を担当する。 | `apps/api/src/rag/memorag-service.ts:280 (usageSummaryFromPage)` |
| 1 | `isInvalidUsageQuery` | is invalid usage query の実装処理を担当する。 | `apps/api/src/routes/admin-routes.ts:728 (isInvalidUsageQuery)` |

## 5. データ・外部境界

| 種別 | 境界 | Target | Operation | 目的 | Caller | 実装位置 |
| --- | --- | --- | --- | --- | --- | --- |
| 参照 | Store | `this.deps.usageEventStore` | `query` | `this.deps.usageEventStore` に対して query を実行する。 | `MemoRagService.listUsageSummaries` | `apps/api/src/rag/memorag-service.ts:2181 (MemoRagService.listUsageSummaries)` |

## 6. 応答・メッセージ

| 種別 | Status/Event | 内容 | 発生条件 |
| --- | --- | --- | --- |
| OpenAPI contract | `200` | リクエストは成功し、レスポンス body に結果を返します。 | OpenAPI で宣言された HTTP 200 response |
| OpenAPI contract | `401` | 認証が必要です。 | OpenAPI で宣言された HTTP 401 response |
| OpenAPI contract | `403` | 対象操作を実行する権限がありません。 | OpenAPI で宣言された HTTP 403 response |
| 例外 | `403` | Forbidden | 利用者が 指定された permission を持たない |
| 例外 | `-` | Invalid half-open usage period | is finite の判定結果が真ではない、または is finite の判定結果が真ではない、または `periodStart` が `periodEnd` 以上である |
| HTTP 実装応答 | `400` | Invalid usage query or cursor | is invalid usage query の判定結果が真である |

## 7. テスト対応

| 関連 | Test case | 実装位置 |
| --- | --- | --- |
| 到達 symbol | shadow usage rollout records events without exposing read or export as the active path | `apps/api/src/rag/memorag-service.test.ts:3101 (shadow usage rollout records events without exposing read or export as the active path)` |
| 到達 symbol | service covers admin defaults, alias misses, terminal async runs, and benchmark edge cases | `apps/api/src/rag/memorag-service.test.ts:3469 (service covers admin defaults, alias misses, terminal async runs, and benchmark edge cases)` |

## 8. 解析上の注意

- 既知の operation 固有 warning はありません。

- 記載内容は route、runtime OpenAPI、handler から到達する repository 内 symbol、既存 test を解析した派生成果物です。
- 可読性のため、詳細設計と unit test の分岐は handler と直下関数（深さ 1 まで）へ絞り、深い共通 helper の内部分岐は API 固有要件として重複掲載しません。
- 文書生成専用 annotation、decorator、コメント、API 別設定表は入力にしていません。
- interface / dynamic dispatch の実装先を TypeScript checker が一意に解決できない場合、logical store call までを確定情報として記載します。
