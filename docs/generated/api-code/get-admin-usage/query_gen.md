<!-- This file is generated by npm run docs:api-code. Do not edit manually. -->

# GET /admin/usage Query・永続化仕様

このアプリケーションは API operation ごとの SQL ファイルを持たないため、handler から到達する store / object / vector / external port の実呼び出しを query 単位として記載します。

## Store・永続化操作

| # | CRUD/実行 | Target | Method | 自然言語での目的 | Caller | コード根拠 |
| ---: | --- | --- | --- | --- | --- | --- |
| 1 | 参照 | `this` | `loadAdminLedger` | `this` に対して load admin ledger を実行する。 | `MemoRagService.listUsageSummaries` | `apps/api/src/rag/memorag-service.ts:917 (MemoRagService.listUsageSummaries)` |
| 2 | 参照 | `this.deps.objectStore` | `getText` | `this.deps.objectStore` に対して get text を実行する。 | `MemoRagService.loadAdminLedger` | `apps/api/src/rag/memorag-service.ts:1515 (MemoRagService.loadAdminLedger)` |
| 3 | 参照 | `this.deps.objectStore` | `listKeys` | `this.deps.objectStore` に対して list keys を実行する。 | `MemoRagService.listDocuments` | `apps/api/src/rag/memorag-service.ts:359 (MemoRagService.listDocuments)` |
| 4 | 参照 | `this.deps.objectStore` | `getText` | `this.deps.objectStore` に対して get text を実行する。 | `MemoRagService.getManifestByKey` | `apps/api/src/rag/memorag-service.ts:1638 (MemoRagService.getManifestByKey)` |
| 5 | 参照 | `this.deps.documentGroupStore` | `list` | `this.deps.documentGroupStore` に対して list を実行する。 | `MemoRagService.listDocuments` | `apps/api/src/rag/memorag-service.ts:371 (MemoRagService.listDocuments)` |
| 6 | 参照 | `this.deps.documentGroupStore` | `list` | `this.deps.documentGroupStore` に対して list を実行する。 | `FolderPermissionService.resolveEffectiveFolderPermissionDetail` | `apps/api/src/folders/folder-permission-service.ts:47 (FolderPermissionService.resolveEffectiveFolderPermissionDetail)` |
| 7 | 参照 | `this.deps.folderPolicyStore` | `get` | `this.deps.folderPolicyStore` に対して get を実行する。 | `FolderPermissionService.resolvePolicyContext` | `apps/api/src/folders/folder-permission-service.ts:128 (FolderPermissionService.resolvePolicyContext)` |
| 8 | 参照 | `this.deps.userGroupStore` | `get` | `this.deps.userGroupStore` に対して get を実行する。 | `FolderPermissionService.resolveUserMembershipPermission` | `apps/api/src/folders/folder-permission-service.ts:166 (FolderPermissionService.resolveUserMembershipPermission)` |
| 9 | 参照 | `this.deps.groupMembershipStore` | `listByGroupId` | `this.deps.groupMembershipStore` に対して list by group id を実行する。 | `FolderPermissionService.resolveUserMembershipPermission` | `apps/api/src/folders/folder-permission-service.ts:171 (FolderPermissionService.resolveUserMembershipPermission)` |
| 10 | 参照 | `objectStore` | `getTextWithVersion` | `objectStore` に対して get text with version を実行する。 | `getTextWithVersion` | `apps/api/src/documents/document-permission-service.ts:418 (getTextWithVersion)` |
| 11 | 参照 | `objectStore` | `getText` | `objectStore` に対して get text を実行する。 | `getTextWithVersion` | `apps/api/src/documents/document-permission-service.ts:419 (getTextWithVersion)` |
| 12 | 参照 | `this.deps.objectStore` | `getText` | `this.deps.objectStore` に対して get text を実行する。 | `DocumentPermissionService.loadLegacyDocumentGrants` | `apps/api/src/documents/document-permission-service.ts:193 (DocumentPermissionService.loadLegacyDocumentGrants)` |
| 13 | 参照 | `this.deps.userGroupStore` | `get` | `this.deps.userGroupStore` に対して get を実行する。 | `DocumentPermissionService.resolveUserMembershipPermission` | `apps/api/src/documents/document-permission-service.ts:287 (DocumentPermissionService.resolveUserMembershipPermission)` |
| 14 | 参照 | `this.deps.groupMembershipStore` | `listByGroupId` | `this.deps.groupMembershipStore` に対して list by group id を実行する。 | `DocumentPermissionService.resolveUserMembershipPermission` | `apps/api/src/documents/document-permission-service.ts:291 (DocumentPermissionService.resolveUserMembershipPermission)` |
| 15 | 参照 | `this.deps.benchmarkRunStore` | `list` | `this.deps.benchmarkRunStore` に対して list を実行する。 | `MemoRagService.listBenchmarkRuns` | `apps/api/src/rag/memorag-service.ts:2249 (MemoRagService.listBenchmarkRuns)` |
| 16 | 参照 | `this.deps.objectStore` | `listKeys` | `this.deps.objectStore` に対して list keys を実行する。 | `MemoRagService.listDebugRuns` | `apps/api/src/rag/memorag-service.ts:1035 (MemoRagService.listDebugRuns)` |
| 17 | 参照 | `this.deps.objectStore` | `getText` | `this.deps.objectStore` に対して get text を実行する。 | `MemoRagService.listDebugRuns` | `apps/api/src/rag/memorag-service.ts:1039 (MemoRagService.listDebugRuns)` |
| 18 | 実行 | `[<br>          stored.lastActivityAt,<br>          ...userBenchmarkRuns.map((run) => run.updatedAt),<br>          ...debugRuns.map((run) => run.completedAt)<br>        ]` | `filter` | `[<br>          stored.lastActivityAt,<br>          ...userBenchmarkRuns.map((run) => run.updatedAt),<br>          ...debugRuns.map((run) => run.completedAt)<br>        ]` に対して filter を実行する。 | `MemoRagService.listUsageSummaries` | `apps/api/src/rag/memorag-service.ts:927 (MemoRagService.listUsageSummaries)` |
| 19 | 実行 | `[<br>          stored.lastActivityAt,<br>          ...userBenchmarkRuns.map((run) => run.updatedAt),<br>          ...debugRuns.map((run) => run.completedAt)<br>        ].filter(Boolean)` | `sort` | `[<br>          stored.lastActivityAt,<br>          ...userBenchmarkRuns.map((run) => run.updatedAt),<br>          ...debugRuns.map((run) => run.completedAt)<br>        ].filter(Boolean)` に対して sort を実行する。 | `MemoRagService.listUsageSummaries` | `apps/api/src/rag/memorag-service.ts:927 (MemoRagService.listUsageSummaries)` |
| 20 | 実行 | `[<br>          stored.lastActivityAt,<br>          ...userBenchmarkRuns.map((run) => run.updatedAt),<br>          ...debugRuns.map((run) => run.completedAt)<br>        ].filter(Boolean).sort()` | `at` | `[<br>          stored.lastActivityAt,<br>          ...userBenchmarkRuns.map((run) => run.updatedAt),<br>          ...debugRuns.map((run) => run.completedAt)<br>        ].filter(Boolean).sort()` に対して at を実行する。 | `MemoRagService.listUsageSummaries` | `apps/api/src/rag/memorag-service.ts:927 (MemoRagService.listUsageSummaries)` |
| 21 | 実行 | `db.users<br>      .filter((user) => user.status !== "deleted")<br>      .map((user) => {<br>        const stored = db.usage[user.userId] ?? {}<br>        const userBenchmarkRuns = benchmarkRuns.filter((run) => run.createdBy === user.userId)<br>        const lastActivityAt = [<br>          stored.lastActivityAt,<br>          ...userBenchmarkRuns.map((run) => run.updatedAt),<br>          ...debugRuns.map((run) => run.completedAt)<br>        ].filter(Boolean).sort().at(-1)<br>        return {<br>          userId: user.userId,<br>          email: user.email,<br>          displayName: user.displayName,<br>          chatMessages: stored.chatMessages ?? 0,<br>          conversationCount: stored.conversationCount ?? 0,<br>          questionCount: stored.questionCount ?? 0,<br>          documentCount: user.groups.includes("RAG_GROUP_MANAGER") \|\| user.groups.includes("SYSTEM_ADMIN") ? documents.length : 0,<br>          benchmarkRunCount: (stored.benchmarkRunCount ?? 0) + userBenchmarkRuns.length,<br>          debugRunCount: user.groups.includes("SYSTEM_ADMIN") ? debugRuns.length : (stored.debugRunCount ?? 0),<br>          lastActivityAt<br>        }<br>      })<br>      ` | `sort` | `db.users<br>      .filter((user) => user.status !== "deleted")<br>      .map((user) => {<br>        const stored = db.usage[user.userId] ?? {}<br>        const userBenchmarkRuns = benchmarkRuns.filter((run) => run.createdBy === user.userId)<br>        const lastActivityAt = [<br>          stored.lastActivityAt,<br>          ...userBenchmarkRuns.map((run) => run.updatedAt),<br>          ...debugRuns.map((run) => run.completedAt)<br>        ].filter(Boolean).sort().at(-1)<br>        return {<br>          userId: user.userId,<br>          email: user.email,<br>          displayName: user.displayName,<br>          chatMessages: stored.chatMessages ?? 0,<br>          conversationCount: stored.conversationCount ?? 0,<br>          questionCount: stored.questionCount ?? 0,<br>          documentCount: user.groups.includes("RAG_GROUP_MANAGER") \|\| user.groups.includes("SYSTEM_ADMIN") ? documents.length : 0,<br>          benchmarkRunCount: (stored.benchmarkRunCount ?? 0) + userBenchmarkRuns.length,<br>          debugRunCount: user.groups.includes("SYSTEM_ADMIN") ? debugRuns.length : (stored.debugRunCount ?? 0),<br>          lastActivityAt<br>        }<br>      })<br>      ` に対して sort を実行する。 | `MemoRagService.listUsageSummaries` | `apps/api/src/rag/memorag-service.ts:922 (MemoRagService.listUsageSummaries)` |

## 外部サービス操作

| # | CRUD/実行 | Target | Method | 自然言語での目的 | Caller | コード根拠 |
| ---: | --- | --- | --- | --- | --- | --- |
| 1 | 実行 | `this` | `syncUserDirectory` | `this` へ sync user directory を実行する。 | `MemoRagService.loadAdminLedger` | `apps/api/src/rag/memorag-service.ts:1556 (MemoRagService.loadAdminLedger)` |
| 2 | 参照 | `this.deps.userDirectory` | `listUsers` | `this.deps.userDirectory` へ list users を実行する。 | `MemoRagService.syncUserDirectory` | `apps/api/src/rag/memorag-service.ts:1563 (MemoRagService.syncUserDirectory)` |

## 解析境界

- store interface call は API 実装が要求する論理操作です。local / DynamoDB / S3 など実 adapter の選択は `createDependencies()` の runtime 設定に従います。
- interface 経由で複数 adapter があり TypeScript symbol が一意にならない場合、実装を推測せず interface call を記載します。
- 文書生成専用 query metadata や API 別 CRUD 定義は参照していません。
