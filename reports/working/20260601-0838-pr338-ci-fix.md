# PR338 CI failure fix report

## 指示

- PR #338 の MemoRAG CI result で失敗している `docs:web-inventory:check` と web coverage test を確認し、修正する。
- リポジトリルールに従い、task md、検証、作業レポート、PR コメントへ反映する。

## 要件整理

- `npm run docs:web-inventory:check` の stale generated docs を解消する。
- `npm exec -w @memorag-mvp/web -- vitest run --coverage` の failing test を解消する。
- 実施した検証だけを pass として記録する。
- 本番 UI に架空 fallback や未実装操作を追加しない。

## 検討・判断

- CI 失敗は session-local evidence の payload/UI 変更に伴う追随漏れと判断した。
- Web inventory は手編集ではなく generator で再生成した。
- `App.test.tsx` は旧 payload の完全一致から、新規 `sessionDocumentContext` と `removedTemporaryScopeIds` を明示的に検証する期待値へ更新した。
- 本番 UI 実装は変更せず、test expectation と generated docs の更新に限定したため、README や durable docs の追加更新は不要と判断した。

## 実施作業

- `tasks/do/20260522-2318-session-local-evidence.md` に CI 失敗の軽量なぜなぜ分析と受け入れ条件を追記した。
- `apps/web/src/App.test.tsx` の chat request payload expectation を現行 contract に合わせた。
- `npm run docs:web-inventory` で `docs/generated/web-*` を再生成した。

## 成果物

- `apps/web/src/App.test.tsx`
- `docs/generated/web-overview.md`
- `docs/generated/web-features.md`
- `docs/generated/web-accessibility.md`
- `docs/generated/web-ui-inventory.json`
- `docs/generated/web-features/chat.md`
- `tasks/do/20260522-2318-session-local-evidence.md`
- `reports/working/20260601-0838-pr338-ci-fix.md`

## 実行した検証

- `npm run docs:web-inventory:check`: pass
- `npm exec -w @memorag-mvp/web -- vitest run --coverage`: pass
- `npm run typecheck -w @memorag-mvp/web`: pass
- `git diff --check`: pass

## 未対応・制約・リスク

- GitHub Actions の再実行結果は、この commit push 後に PR 上で確認が必要。
- API/infra/benchmark の CI はユーザー提示結果で pass しており、今回の追加修正範囲が web test と generated docs に限定されるため再実行していない。
- `gh-fix-ci` の検査スクリプトは一度 `gh auth status` の token invalid で停止したが、直後の `gh auth status` と `gh pr diff` は成功した。ローカル再現で失敗原因は確認済み。

## Fit 評価

- 総合fit: 4.6 / 5.0
- 理由: 提示された 2 つの CI failure はローカルで再現・修正・再検証できた。GitHub Actions の post-push 再実行結果は外部 CI 完了待ちのため満点ではない。
