# memorag root lift plan report

## 指示

- `memorag-bedrock-mvp/` 配下に一段ネストされている MVP 本体を repository root に引き上げる場合の影響範囲を見積もる。
- 提示された方針に沿って `/plan` として対応する。

## 要件整理

| 要件ID | 要件 | 対応状況 |
|---|---|---|
| R1 | root 化の影響範囲を具体化する | 対応 |
| R2 | 既存ファイル実体に基づいて修正対象を確認する | 対応 |
| R3 | 実装ではなく plan として止める | 対応 |
| R4 | 未実施の検証を実施済みとして書かない | 対応 |

## 検討・判断

- `/plan` 指示のため、worktree 作成、ファイル移動、Taskfile 統合、commit、PR 作成は実施しない方針とした。
- `skills/worktree-task-pr-flow/SKILL.md` を確認し、plan-only request は full workflow を強制しない例外に該当すると判断した。
- `rg` と `find` により、root から `memorag-bedrock-mvp` を参照する workflow、Taskfile、README、docs、CDK buildspec の実体を確認した。
- 提示された見積もりに加え、`.github/workflows/memorag-openapi-docs.yml` と `infra/lib/memorag-mvp-stack.ts` 内の CodeBuild buildspec が直接影響範囲に入ると判断した。

## 実施作業

- `skills/worktree-task-pr-flow/SKILL.md` と `skills/post-task-fit-report/SKILL.md` を確認した。
- root / nested の `Taskfile`、`package.json`、`.gitignore`、workflow 参照を確認した。
- `memorag-bedrock-mvp` 文字列の主要参照箇所を検索した。

## 成果物

| 成果物 | 内容 |
|---|---|
| 最終回答本文 | root 化の影響範囲、推奨方針、移行手順、検証計画 |
| `reports/working/20260513-0000-root-lift-plan.md` | 今回の調査・判断の作業レポート |

## Fit 評価

総合fit: 4.6 / 5.0

理由: `/plan` として必要な影響範囲と移行方針は整理した。実装、検証コマンド実行、CI 実行は今回の指示範囲外として未実施であるため満点ではない。

## 未対応・制約・リスク

- 実際の `git mv`、Taskfile 統合、workflow 修正、docs 修正は未実施。
- `npm ci`、`npm run ci`、`task verify`、`task cdk:test`、`task cdk:synth:yaml`、`docker compose config` は未実行。
- 既存の未追跡ファイルが `reports/working/` と `tasks/done/` に存在するため、実装 PR 作業時は専用 worktree で切り離す必要がある。
