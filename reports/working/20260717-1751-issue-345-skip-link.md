# 作業完了レポート

保存先: `reports/working/20260717-1751-issue-345-skip-link.md`

## 1. 受けた指示

- Issue #345 を Draft stacked PR #420 final head `f6a0e92353fef9631160fa2a92f6c837e3d71bb3` から継続する。
- #420 の text spacing と既存 keyboard 作業を重複させず、未検証の最小 production-quality unit を選ぶ。
- 専用 worktree/task lifecycle、明示 AC/Done、実装・検証、日本語 gitmoji commit、Draft PR、semver label、日本語 AC/セルフレビューコメント、final-head CI、Issue 進捗、clean/upstream まで進める。
- merge/deploy/release を行わず、screen reader/real-device/browser gap を正直に残す。

## 2. 要件整理

| 要件ID | 指示・要件 | 重要度 | 対応状況 |
| --- | --- | ---: | --- |
| R1 | #420 final head から専用 worktree/branch を作る | 高 | 対応 |
| R2 | text spacing / 既存 keyboard journey と非重複の gap を選ぶ | 高 | 対応 |
| R3 | skip link と main landmark focus target を production UI に実装する | 高 | 対応 |
| R4 | desktop/mobile Chromium の自動証跡を追加する | 高 | 対応 |
| R5 | `SQ-016`、UI design、trace、generated inventory を同期する | 高 | 対応 |
| R6 | 最小十分な Web/E2E/docs 検証を実施する | 高 | 対応 |
| R7 | manual/cross-browser gap を pass と扱わない | 高 | 対応 |
| R8 | PR/task/report/GitHub/CI lifecycle を完了する | 高 | PR 作成以降を継続中 |

## 3. 検討・判断したこと

- #385 は landmark/contrast/target、#396 は primary navigation の keyboard-only journey、#400 は Chromium AX tree、#404 は touch activation、#408〜#420 は zoom/layout/viewport/text spacing を担当する。
- stack head には反復 navigation を迂回する skip link がなく、`AC-SQ016-002` の skip/recovery に専用証跡もないため、bypass navigation を次の bounded unit とした。
- native anchor を優先し、layout root、navigation、main landmark を sibling として分離した。focus target は `main#main-content[tabindex="-1"]` とした。
- App の canonical route 正規化が activation 後の fragment hash を除去することを targeted E2E で確認した。URL hash の永続化は要求せず、path/search を変えず main focus が維持される契約を採用した。
- 正規要件の意味は変更せず、現在の自動 evidence と design/trace/generated output を同期した。

## 4. 実施した作業

- `codex/issue-345-skip-link` worktree/branch を #420 final head から作成した。
- `tasks/do/20260717-1742-issue-345-skip-link.md` に軽量 RCA、AC、Done、検証計画を実装前に記録した。
- `AppShell` の outer layout root を `div`、主領域を一意な `main` landmark に変更し、rail より前に「メインコンテンツへ移動」link を追加した。
- skip link を通常時は viewport 外、focus-visible 時は既存 token と 3px outline で viewport 内に表示する CSS を追加した。
- 1280×720 / 320×720 で最初の Tab、visible focus、Enter activation、main focus、landmark 一意性、navigation separation、URL、overflow、JSON evidence を確認する `E2E-UI-SKIP-001` を追加した。
- `SQ-016`、`DES_UI_UX_001`、UI trace manifest と generated Web inventory/trace を同期した。

## 5. 成果物

| 成果物 | 形式 | 内容 | 指示との対応 |
| --- | --- | --- | --- |
| `apps/web/src/app/AppShell.tsx` | React | skip link と main landmark 分離 | R3 |
| `apps/web/src/styles/layout.css` | CSS | off-screen / focus-visible 表示 | R3 |
| `apps/web/e2e/skip-link.spec.ts` | Playwright | desktop/mobile bypass journey と evidence | R4 |
| `REQ_SERVICE_QUALITY_016.md` / `DES_UI_UX_001.md` | Markdown | 正規要件 evidence / UI design 同期 | R5 |
| `tools/web-inventory/ui-traceability.json` / `docs/generated/web-*` | JSON/Markdown | `E2E-UI-SKIP-001` と UI inventory 同期 | R5 |
| `tasks/do/20260717-1742-issue-345-skip-link.md` | Markdown | RCA、AC、Done、検証記録 | R1/R2/R8 |

## 6. 検証結果

### 実行した検証

- `npm ci`: pass。504 packages、既存 audit 8件（low 2 / moderate 1 / high 5）を報告。
- `npm run test:e2e:smoke -w @memorag-mvp/web -- --grep E2E-UI-SKIP-001`: 初回は worktree dependency 不在により親 checkout の stale contract package を参照して API 起動前 fail。`npm ci` 後の初回は fragment hash 期待が canonical normalization と不一致で 2 fail。focus/URL 契約を実挙動へ合わせて修正後 2/2 pass。
- `npm run test:e2e:smoke -w @memorag-mvp/web`: 23/23 pass。
- `npm run test:e2e:all -w @memorag-mvp/web`: 35/35 pass。
- `npm run test -w @memorag-mvp/web`: 61 files / 442 tests pass。
- `npm run typecheck -w @memorag-mvp/web`: pass。
- `npm run build -w @memorag-mvp/web`: pass。既知の 500 kB 超 chunk warning あり。
- `npm run lint`: pass。
- `npm run test:web-semantic-ui`: pass。
- `npm run docs:web-trace:test`: 8 tests pass。
- `npm run docs:web-inventory:check`: pass。
- `task docs:check`: pass。実行前に resolved command を確認済み。
- `git diff --check`: pass。

### 未実施・制約

- representative screen reader: 利用可能な承認済み環境と手動実施者がないため未実施。
- real device / OS keyboard / browser chrome: 実機環境がないため未実施。
- Firefox / WebKit: Issue #345 の approved required/scheduled scope が未確定で、現 Playwright project は Chromium のみのため未実施。
- manual browser keyboard: Playwright Chromium 自動操作を実施したが、独立した手動実測は未実施。

## 7. 指示への fit 評価

| 評価軸 | 評価 | 理由 |
| --- | ---: | --- |
| 指示網羅性 | 4.8/5 | PR/CI/lifecycle はこのレポート後に継続するが、実装・検証要件は満たした |
| 制約遵守 | 5.0/5 | 非重複 unit、stack base、非 merge/deploy/release、manual gap 表示を維持 |
| 成果物品質 | 4.8/5 | native semantics、production fix、targeted/full regression、docs trace を同期 |
| 説明責任 | 5.0/5 | 初回失敗、既知 warning、未実施範囲を区別 |
| 検収容易性 | 5.0/5 | AC、E2E ID、JSON evidence、コマンド結果を追跡可能 |

**総合fit: 4.9/5（約98%）**

理由: 選定した bounded unit の production 実装、自動証跡、正規 docs/generated 同期は完了した。screen reader/real-device/cross-browser は意図的に未完了として残し、PR/GitHub/CI lifecycle は同 branch でこの後に完了させる。

## 8. 未対応・制約・リスク

- 未対応: representative screen reader、real device、Firefox/WebKit、独立 manual keyboard evidence。
- 制約: Playwright config は Chromium 1 project であり、未承認 browser matrix を本 unit で拡張していない。
- リスク: browser/screen-reader による native fragment focus と route normalization の細部差は残る。Chromium では main focus と canonical URL 維持を確認済み。
- 既存リスク: npm audit 8件と Web build chunk warning は dependency/bundle 非変更のため本 task で修正していない。

## 9. ドキュメント更新判断

- 更新: `SQ-016`、`DES_UI_UX_001`、trace manifest、generated Web inventory/trace。production behavior と verification evidence が変わるため同変更単位で同期した。
- 更新不要: README、API examples、OpenAPI、operations、AGENTS。公開 API、setup、運用手順、agent policy は変更していない。
